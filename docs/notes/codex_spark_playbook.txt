ULTRA-PERFORMANCE PYTHONIC LANGUAGE — CODEX 5.3-CODEX-SPARK EXECUTION PLAYBOOK (Ubuntu)
Version: 0.1 (living document)
Audience: Codex 5.3-Codex-Spark acting as a senior compiler/runtime engineer.
Primary constraint: Build time is cheap; runtime performance is expensive.
Primary deliverable: A new language + compiler + runtime that feels Python/MATLAB-simple but hits near-hardware limits for list/matrix-heavy workloads.

-------------------------------------------------------------------------------
0) YOUR ROLE (CODEX) + OPERATING MODE
-------------------------------------------------------------------------------
You (Codex) are an engineering agent. Execute this playbook in phases. Do not jump ahead.
For each phase:
  - Create a short “Phase Plan” (tasks, choices, risks).
  - Implement or prototype what is required.
  - Run the verification checklist.
  - Produce a “Phase Report” with measured results and a GO/NO-GO recommendation.
  - STOP and ask the human to approve moving to the next phase.

Rules:
  - Prefer primary sources (papers, official docs) for design decisions.
  - Prefer correctness and measured speedups over speculation.
  - Maintain a reproducible benchmark harness from Phase 1 onward.
  - “Fast” means: fewer allocations, fewer passes, fewer branches, fewer cache misses, higher bandwidth utilization.
  - Assume multi-CPU targets (x86_64, aarch64, riscv64). Optimize for baseline + multiversion dispatch.
  - Never introduce “performance hacks” that can silently change numerical results without a verification gate.

-------------------------------------------------------------------------------
1) REQUIREMENTS (EXTRACTED FROM FULL CONVERSATION) — DO NOT DROP ANY
-------------------------------------------------------------------------------
User-visible goals (non-negotiable):
  [R1] Python-like syntax (close to Python; MATLAB-like ease for math).
  [R2] Multiparadigm: OOP + Functional + Data-Oriented + Event-Driven built-in.
  [R3] Classes can accept variable/optional inputs like Python (dynamic-ish).
  [R4] First-class math: matrices/lists are central; extremely fast numeric/data workloads.
  [R5] Built-in float types: f8, f16, f32, f64, f128, f256, f512 (and optionally bf16).
  [R6] Primitive baseline: Int (at least i64) and Double (f64) exist and are fast.
  [R7] A single “List” primitive can hold primitives + objects; supports homo/hetero.
  [R8] Object create/access/replace/delete/copy should be near-zero cost when feasible.
  [R9] Language is natively parallel + async-friendly (low overhead concurrency).
  [R10] Memory management must be “innovative” and a differentiator.
  [R11] Runs on (almost) every CPU without serious performance loss.
  [R12] Build time is not important; runtime performance is paramount.
  [R13] Functional pipeline performance must be maximal (fusion, no intermediates).
  [R14] Matrix/list ops: indexing, iteration, replace, delete, matmul, slicing must avoid slowdowns.
  [R15] Heterogeneous lists/matrices must “work”, but fast paths for homogeneous must hit near-max.
  [R16] If a technique risks wrong results, either (a) prove/safeguard it in compile-time layers or
       (b) don’t use it for the “fast/perf tier”.

Engineering constraints (derived):
  [E1] We can create multiple layers/tiers (T0..Tn). Each tier has one main responsibility.
  [E2] Heavy compile-time analyses are allowed: whole-program, auto-tuning, profile-guided, multi-versioning.
  [E3] Target portability must not rely on a slow “one-size-fits-all” runtime; prefer AOT per target + dispatch.

-------------------------------------------------------------------------------
2) REALITY CHECK — “DOABLE” VS “NOT GUARANTEED / PHYSICALLY LIMITED”
-------------------------------------------------------------------------------
You must keep expectations realistic while still pushing aggressively.

Doable:
  - Be 2×–10× faster than *naïve* C for many list/matrix pipelines by eliminating passes/allocations
    (fusion), improving locality, and auto-scheduling (inspired by Weld/stream fusion/Halide/TVM).
  - Reach parity with optimized C for many scalar/array loops (LLVM+LTO+PGO, good layouts).
  - Approach vendor-library performance on some kernels via tiling + auto-tuning; or call vendor libs.

Not guaranteed (treat as “cannot guarantee across all workloads”):
  - “Always 4–5× faster than the best hand-tuned C/ASM for every program.”
    Peak is bounded by memory bandwidth, cache, ISA, and algorithmic limits.
  - “Zero overhead” for fully general heterogeneity with arbitrary per-element operations.
    You can move overhead out of hot loops via analyze+plan+materialize+cache, but not remove it universally.
  - f128/f256/f512 being as fast as f64 on commodity CPUs (most will be software-emulated).
    Provide them for correctness/precision; don’t promise speed.

-------------------------------------------------------------------------------
3) ACADEMIC/THEORETICAL BACKBONE — WHAT WE’RE BUILDING ON (READ + STEAL IDEAS)
-------------------------------------------------------------------------------
You must build the design around proven concepts. Use these as anchors:

(Compilation + IR)
  - MLIR (multi-level IR with extensible dialects; helps heterogenous targets and domain lowering). 
    Ref: https://mlir.llvm.org/
  - Multi-level verification dialects and tooling ideas (robustness for evolving IR stacks).
    Ref: pldi25 “First-Class Verification Dialects for MLIR” (Fehr et al.)

Array/pipeline optimization & schedule separation
  - Halide: separate “algorithm” from “schedule”; scheduling should not change semantics.
    Ref: halide12 paper (Ragan-Kelley et al.), and formal semantics work.
  - Weld: a common IR/runtime that fuses across library boundaries; big gains from reduced data movement.
    Ref: CIDR’17 Weld paper (Palkar et al.), and follow-up works.
  - Stream Fusion: deforestation for list pipelines; eliminate intermediate lists.
    Ref: Coutts/Leshchinskiy/Stewart (2007), and follow-ups.

Functional array languages / performance portability
  - Futhark: purely functional data-parallel arrays; race-free semantics; compiler transforms for performance.
    Ref: PLDI’17 Futhark paper + thesis/book.

Kernel / tiling compilers (matrix-heavy)
  - Triton: tiled IR/compiler for matrix/tensor kernels; near vendor performance in some cases.
    Ref: Tillet et al. (2019).
  - TVM: end-to-end optimizing compiler; graph+operator fusion; auto-tuning/cost models.
    Ref: Chen et al. (OSDI’18 / arXiv).

Dynamic objects at speed (object model)
  - V8 hidden classes (“Maps”) + inline caching: optimize property access by stabilizing shapes.
    Ref: https://v8.dev/docs/hidden-classes and related shape/IC explanations.

Memory management theory
  - Region-based memory management (Tofte & Talpin, 1997).
  - Cyclone regions (Grossman et al., 2002) and modern region/effect variants (e.g., Spegion 2025).

Parallelism + scheduling
  - Cilk work-stealing scheduler: efficient for well-structured parallelism; provable bounds.
    Ref: Blumofe/Leiserson (1996), and later work.
  - Structured concurrency (Swift, Kotlin): lifecycle-scoped tasks, cancellation, safety by structure.
    Refs: Swift concurrency docs, Kotlin coroutine docs.

Profile-guided + post-link optimization (build-time heavy is OK)
  - AutoFDO (sample-based profiling -> FDO), Propeller (profile-guided relinking), BOLT (post-link optimizer).
    These yield single-digit to sometimes double-digit % improvements beyond PGO/LTO in practice.

-------------------------------------------------------------------------------
4) CORE DESIGN DECISION (RECOMMENDED): “PYTHONIC SURFACE, STATIC PERF CORE”
-------------------------------------------------------------------------------
To reconcile Python feel with C-class performance, use a tiered design:

T8: Surface language (Pythonic)
  - Dynamic-feel syntax, flexible classes, convenience features.
  - Allowed: hetero lists, open objects, dynamic calls.
  - Promise: “it runs” + good defaults.

T6: Adaptive runtime tier
  - Performs analyze+plan steps for container layouts (homogeneous fast paths).
  - Inserts guards once; never per-element checks in hot loops if avoidable.

T4: Performance tier (“Perf Core”)
  - Hot code must be stabilizable: known types/shapes/contiguity/effects.
  - Disallows or strongly constrains features that prevent specialization.
  - Goal: generate predictable, low-level code comparable to C/Fortran.

T2: Kernel tier
  - Very small “unsafe-ish” subset used by the compiler to express kernels.
  - Any UB risk is contained here, never exposed to surface.

T0: FFI/Intrinsics (optional)
  - Optional for truly last-mile tuning (intrinsics, asm), but keep minimal.

Key idea:
  - Users write T8.
  - Compiler + analyzer lowers to T4/T2 for hot regions automatically.
  - When impossible, it falls back to slower paths *explicitly* and transparently (with diagnostics).

-------------------------------------------------------------------------------
5) LIST + MATRIX: MULTI-REPRESENTATION WITH STATE MACHINE (THE PERFORMANCE CENTERPIECE)
-------------------------------------------------------------------------------
Design List/Matrix as “one semantic type, many physical reps.”

List semantic API:
  - indexing: x[i]
  - iteration: for v in x
  - update: x[i] = ...
  - append/pop/insert/delete/slice
  - map/filter/reduce/zip/scan (must fuse when possible)

Physical reps (choose dynamically or statically):
  (L1) Packed[T]          : unboxed, contiguous, homogeneous
  (L2) PackedNumeric[U]   : promoted numeric common type (e.g., i64->f64)
  (L3) ChunkedUnion       : segments per type (good for iter-heavy hetero)
  (L4) Boxed[Any]         : tagged cells / pointers (general fallback)
  (L5) GatherScatterView  : typed views + index map (preserve order; fast kernels)
  (L6) SmallList          : inline small-vector optimization (avoid heap for tiny lists)

State machine transitions (core rule):
  - Never pay per-element tag checks inside hot loops if a one-time analyze+materialize can avoid it.
  - “Analyze once, cache plan”:
      layout = analyze(x)
      plan   = choose(layout, op)
      x_rep  = materialize_if_needed(x, plan)
      run fast kernel over x_rep
      cache(x.layout_state = x_rep)

Matrix reps:
  (M1) Matrix[T] contiguous (row-major or column-major; pick one canonical; allow views/strides)
  (M2) MatrixView[T] (slice/transpose) with strides; lower to contiguous when needed
  (M3) BlockedMatrix[T] (tiling-friendly internal layout)
  (M4) Sparse variants (future; optional, inspired by Taichi sparse structures)

Rule for max performance:
  - Matmul/conv/BLAS-like ops should operate on contiguous/blocked typed reps.
  - If surface code creates hetero “matrix”, enforce an explicit normalize (or error in perf tier).

-------------------------------------------------------------------------------
6) OBJECT MODEL: “VALUE/SHAPE FIRST” FOR NEAR-ZERO COST
-------------------------------------------------------------------------------
Goal: make object creation and field access cheap by default.

Three object modes (one semantic class system, multiple runtime reps):
  (O1) Value Object (inlined / unboxed)
      - stored directly in arrays/stack when it doesn’t escape.
      - best for data-oriented and numeric-heavy code.
  (O2) Handle Object (stable id/handle, movable storage)
      - good locality; compaction possible; 1 indirection.
  (O3) Open Object (dict/shape-changing)
      - maximum flexibility; slow path.

Shape system (borrow from V8 “Maps”):
  - Each object carries a ShapeID.
  - Field accesses compile to: guard(shape==X) then load at known offset.
  - If megamorphic site: fallback to dictionary lookup or polymorphic inline cache.

Key optimization gates:
  - Escape analysis decides Value vs Handle vs Open.
  - “Perf tier” restricts frequent shape changes; encourages sealed/slot-like objects.

-------------------------------------------------------------------------------
7) MEMORY MANAGEMENT: “HYBRID REGIONS + GENERATIONAL + ARENAS”
-------------------------------------------------------------------------------
You must differentiate on memory; the user demanded innovation.

Recommended hybrid strategy:
  - Fast arenas / bump allocators for short-lived allocations (expression temps, pipeline intermediates).
  - Region inference or effect-based regions for large scopes (Tofte/Talpin lineage; Cyclone/Spegion ideas).
  - Generational GC OR deferred refcounting ONLY for objects that truly need tracing.
  - Pool allocators for uniform small objects.
  - Copy/move compaction for handle objects to improve locality (optional; advanced).

How this supports performance:
  - Pipeline fusion reduces allocations; remaining allocations go to bump/region -> near-zero cost.
  - Escape analysis + regionization means most temps never hit general heap.
  - Hetero containers can normalize into packed buffers allocated in arenas.

Correctness gates:
  - Debug builds run sanitizers (ASan/UBSan/TSan where applicable).
  - Perf tier forbids undefined behaviors; unsafe ops are isolated in T2 kernel tier.

-------------------------------------------------------------------------------
8) PARALLEL + ASYNC + EVENT-DRIVEN: “STRUCTURED + WORK-STEALING”
-------------------------------------------------------------------------------
Requirements: built-in parallelism and async that doesn’t kill performance.

Model:
  - Structured concurrency for async tasks (Swift/Kotlin style):
      * tasks live inside scopes
      * cancellation propagates
      * clear lifetimes
  - Work-stealing scheduler for fine-grained parallel tasks (Cilk style).
  - Async/await compiles to state machines; avoid allocations for state machines when possible.
  - Event loop primitives (channels, streams, async iterators).

Safety/perf tier:
  - “Sendable”-like trait/effect: what can cross threads.
  - Ownership/uniqueness/effect system to prevent data races in perf-tier code.
  - Zero-cost when used sequentially (no forced runtime checks in hot loops).

-------------------------------------------------------------------------------
9) PORTABILITY WITHOUT BIG PERF LOSS
-------------------------------------------------------------------------------
Approach:
  - Use LLVM/MLIR to generate native code for multiple architectures.
  - Produce “fat binaries”:
      * baseline (portable ISA subset)
      * tuned variants (AVX2/AVX-512 on x86; NEON/SVE on arm; etc.)
      * runtime dispatch based on CPUID/HWCAP.
  - For “runs everywhere” fallback, optionally support WebAssembly as a *compat mode*,
    but do NOT claim it matches native peak everywhere.

-------------------------------------------------------------------------------
10) PERFORMANCE TOOLCHAIN: USE BUILD-TIME HEAVY OPTIONS
-------------------------------------------------------------------------------
Because build time is free, use:
  - LTO/ThinLTO
  - PGO (instrumentation or sampling)
  - Post-link optimization (BOLT)
  - Profile-guided relinking (Propeller-like workflow)
  - Sample-based FDO (AutoFDO concepts)
This can yield single-digit % improvements on top of already-optimized builds (sometimes more).

-------------------------------------------------------------------------------
11) BENCHMARKS + METRICS (GATING FOR EVERY PHASE)
-------------------------------------------------------------------------------
You must maintain a benchmark suite that covers:
  (B1) scalar loops (int/double)
  (B2) packed list iteration, map/reduce
  (B3) hetero list: iterate, map, normalize then compute
  (B4) matrix: matmul (various sizes), transpose, slicing views, reductions
  (B5) pipeline: map->filter->map->reduce with fusion expected
  (B6) object ops: create/access/update/copy/delete in tight loops
  (B7) async/event: spawn tasks, channel throughput, cancellation overhead
  (B8) parallel map/reduce: scaling curve

Metrics to record:
  - wall time, ns/op
  - cycles/element, instructions/element
  - branch-misses, cache-misses (L1/L2/LLC), bandwidth (GB/s)
  - allocations/op, bytes allocated/op, peak RSS
Tools:
  - hyperfine, perf stat, perf record + flamegraph, heaptrack, valgrind/callgrind (as needed)

Baselines:
  - C baseline compiled with clang -O3 (and -march=native for “best local” reference)
  - Python baseline + NumPy for matrix/array ops (as a productivity reference)
  - For matmul: compare to BLAS (OpenBLAS/BLIS/MKL if available) as “near-peak” yardstick.

-------------------------------------------------------------------------------
12) PROJECT PLAN — 10 PHASES WITH EXIT CRITERIA
-------------------------------------------------------------------------------
IMPORTANT: Do not proceed to the next phase without passing the exit criteria and obtaining user approval.

PHASE 1 — Foundation: repo, toolchain, baselines, research notebook
Goal:
  - Create a reproducible environment and performance baseline.
Tasks:
  1) Create git repo with /compiler, /runtime, /stdlib, /tests, /bench, /docs.
  2) Pick implementation language for compiler:
       Option A (recommended): C++20 + MLIR/LLVM (best integration, best optimization ecosystem).
       Option B: Rust front-end -> emit MLIR text; use LLVM/MLIR tools for lowering (harder plumbing).
  3) Add build system (CMake + Ninja) and CI (GitHub Actions or local scripts).
  4) Write benchmark harness with:
       - C baselines
       - Python baselines (optional)
       - scaffolding for new language runs
  5) Create “research notes” doc summarizing key papers (MLIR/Halide/Weld/Futhark/StreamFusion).
Verification checklist:
  - `bench/` runs and records baseline numbers in a versioned file (CSV/JSON).
  - `perf stat` scripts run without manual tweaking.
Exit criteria:
  - Repo builds on Ubuntu; baseline results are reproducible (±3% variance across runs).

PHASE 2 — Front-end v0: Python-like syntax parser + AST + minimal evaluator
Goal:
  - Parse and run a tiny subset (correctness first).
Scope:
  - functions, let/assign, if/while/for, int/double literals, list/matrix literals, indexing.
Tasks:
  1) Define grammar (document it).
  2) Implement lexer/parser (handwritten or using a parser generator).
  3) Implement AST + pretty-printer.
  4) Minimal interpreter OR direct lowering to a very simple IR for testing.
Verification checklist:
  - 100+ unit tests for parsing and evaluation.
  - Run sample programs resembling Python syntax.
Exit criteria:
  - Correctness tests pass; parser stable; no ambiguity in list/matrix literal syntax.

PHASE 3 — Type/Shape system + Tier boundaries (T8->T4 lowering)
Goal:
  - Make “fast subset” explicit and machine-checkable.
Tasks:
  1) Implement type system:
       - Int (i64), Double (f64), Float<N> family, Bool, String (optional),
         List, Matrix, Class types, Any.
       - Type inference where possible; explicit annotations allowed.
  2) Implement “ShapeID” mechanism for classes (sealed vs open).
  3) Implement tier annotations or compiler-driven hot-region marking.
  4) Diagnostics: explain why code cannot be lowered to perf tier.
Verification checklist:
  - Type inference tests.
  - Shape stabilization tests (same fields -> same shape).
Exit criteria:
  - Compiler can label regions as “Perf-tier eligible” or “needs fallback” with clear reasons.

PHASE 4 — Codegen v0 (scalar): LLVM backend + runtime launcher
Goal:
  - Generate native code for scalar code paths.
Tasks:
  1) Choose IR strategy:
       - Option A: Lower to MLIR first, then to LLVM dialect, then LLVM IR.
       - Option B: Direct LLVM IR from your own IR (faster to start, less future-proof).
     Recommendation: Option A if you commit to MLIR stack; Option B only for prototyping.
  2) Implement codegen for ints/doubles, control flow, function calls.
  3) Implement runtime launcher (JIT or AOT; AOT preferred for reproducibility).
Verification checklist:
  - Run scalar benchmarks and compare vs clang -O3.
Exit criteria:
  - On scalar loops, within 0.9×–1.2× of clang -O3 (same algorithm).

PHASE 5 — Containers v1: Packed[T] list + Matrix[T] + bounds proof hooks
Goal:
  - “Homogeneous fast path” must be excellent.
Tasks:
  1) Implement Packed[T] list:
       - contiguous, unboxed, aligned
       - amortized append strategy
  2) Implement Matrix[T]:
       - contiguous storage
       - views/slices as stride-based references
  3) Add bounds-check elimination hooks:
       - range analysis in loops
       - hoist checks or prove safe
Verification checklist:
  - Bench B2/B4 on packed data.
Exit criteria:
  - Packed list iteration/map/reduce is within 0.85×–1.15× of equivalent C loops.
  - Matrix elementwise ops within 0.85×–1.15× of C loops.

PHASE 6 — Containers v2: heterogeneity without killing hot loops
Goal:
  - Hetero lists “work”, but hot loops pay almost nothing after normalization.
Tasks:
  1) Implement Boxed[Any] representation (tagged cells / handles).
  2) Implement analyze+plan+materialize+cache for list ops.
  3) Implement promotion for numeric heterogeneity (PackedNumeric[U]).
  4) Implement ChunkedUnion for iter-heavy hetero patterns.
  5) Implement GatherScatter views for order-preserving typed kernels.
Verification checklist:
  - Bench B3 and show:
       * first run may pay normalize cost
       * subsequent runs are fast (cached rep)
Exit criteria:
  - For repeated operations on the same hetero list, steady-state performance approaches packed case
    (target: within 1.2×–1.8× depending on mix and required semantics).

PHASE 7 — Functional pipelines: fusion IR + no-intermediate execution
Goal:
  - Functional style must be *fast by default*.
Tasks:
  1) Introduce Array/Pipeline IR (Weld/stream-fusion inspired):
       - represent map/filter/reduce/zip/scan at a high level
       - enable fusion and loop transformations
  2) Implement fusion passes:
       - remove intermediates
       - push filters into producers
       - deforest list pipelines where safe
  3) Materialization rules:
       - only materialize when user observes container identity or needs random access
Verification checklist:
  - Bench B5: pipeline should allocate ~0 intermediates (or bounded minimal).
Exit criteria:
  - Pipeline throughput beats non-fused baseline by 2×–10× on large data (when baseline allocates intermediates).
  - Allocation count for pipeline is near-zero or strictly bounded.

PHASE 8 — Matrix kernel engine: tiling + scheduling (+ optional auto-tuning)
Goal:
  - Matmul and core kernels are competitive.
Tasks:
  1) Implement schedule abstraction (Halide-style): algorithm separate from schedule.
  2) Provide default schedules + auto-tuner (TVM-style search) — build-time is allowed to be heavy.
  3) Implement matmul:
       - blocking/tiling, packing, cache-aware layout
       - support MatrixView lowering when needed (copy-on-write/pack)
  4) (Optional) Call BLAS initially, then replace with generated kernels.
Verification checklist:
  - Bench B4 (matmul sizes).
Exit criteria (choose one path):
  Path A (own kernels): achieve within ~1.2×–2.0× of OpenBLAS on common sizes initially, improve over time.
  Path B (hybrid): match BLAS for matmul by calling it, while your compiler still fuses surrounding ops to reduce overhead.

PHASE 9 — Concurrency: structured async + work stealing + safety rules
Goal:
  - Async and parallelism are first-class, low overhead, safe in perf tier.
Tasks:
  1) Implement async/await lowering to state machines.
  2) Implement structured concurrency scopes and cancellation.
  3) Implement a work-stealing scheduler for parallel tasks.
  4) Implement Send/Sync-like rules via effects/ownership for perf tier.
Verification checklist:
  - Bench B7/B8 for overhead and scaling.
Exit criteria:
  - Task spawn/join overhead is competitive (measure and record).
  - Parallel map scales reasonably on multicore (document scaling curve).

PHASE 10 — Portability + “final-mile” optimization + hardening
Goal:
  - Ship multi-arch builds and squeeze remaining performance.
Tasks:
  1) Multi-target builds (x86_64, aarch64, riscv64) + baseline ISA profile.
  2) Multiversion dispatch for CPU features (build multiple variants).
  3) PGO + LTO + post-link BOLT integration.
  4) Fuzzing + sanitizers + differential testing vs reference implementations.
  5) Documentation: language spec, tier rules, performance model, memory model.
Verification checklist:
  - Build and run on at least 2 architectures (local or CI).
  - Show measured speedup from PGO/BOLT (even single-digit % is fine if consistent).
Exit criteria:
  - Release candidate with reproducible builds, test suite passing, and benchmark report.

-------------------------------------------------------------------------------
13) COMPARISON: WHAT TO “STEAL” FROM POPULAR LANGUAGES (ACTIONABLE)
-------------------------------------------------------------------------------
Rust:
  - Ownership/borrowing to reduce aliasing and enable safe concurrency without GC.
  - Takeaway: use compile-time rules/effects to unlock optimization (bounds/alias elimination).
Zig:
  - Explicit allocator passing + compile-time evaluation (“comptime”) for specialization.
  - Takeaway: make allocation strategy explicit in perf tier; use compile-time specialization.
Julia:
  - Specialization to reconcile dynamism and performance; multiple dispatch as a performance tool.
  - Takeaway: specialization is powerful, but hetero containers cause dynamic dispatch slowdowns; mitigate via normalization.
Chapel:
  - Parallelism as a first-class language feature for productivity at scale.
  - Takeaway: provide high-level parallel constructs that lower to efficient runtimes.

Swift/Kotlin:
  - Structured concurrency; clear task lifetimes; cancellation semantics.
  - Takeaway: integrate structured concurrency to keep async correct and predictable.

-------------------------------------------------------------------------------
14) “DO NOT FORGET” CHECKLIST (FROM CONVERSATION)
-------------------------------------------------------------------------------
Before declaring success, confirm:
  - Python-like syntax is not an afterthought; it is the default.
  - Lists/matrices are primitives with top-tier performance.
  - Hetero lists have a fast steady state via caching normalized reps.
  - Object ops can be near-zero cost via value/handle objects and arenas.
  - Async + parallel are language features, not bolted-on libraries.
  - Portability strategy does not collapse performance (use AOT multi-target + dispatch).
  - Correctness gates exist for any aggressive optimization.

-------------------------------------------------------------------------------
15) NEXT STEP FOR YOU (CODEX) RIGHT NOW
-------------------------------------------------------------------------------
Start PHASE 1.
Output:
  - Proposed repo structure
  - Toolchain choice (C++/MLIR vs alternatives) with rationale
  - Benchmark plan and baseline measurement script
  - Exact commands to build/run on Ubuntu
Then implement it and provide the PHASE 1 REPORT.

END OF PLAYBOOK

-------------------------------------------------------------------------------
14) PHASE 5 EXECUTION LOG (IMPLEMENTATION DIARY)
-------------------------------------------------------------------------------
- Added matrix elementwise operator typing in Phase 3 checker to align type diagnostics before codegen.
- Added list mutator typing/call handling for append/pop/insert/remove.
- Added container semantics tests for list mutation methods and matrix elementwise arithmetic.
- Result: semantic coverage improved; remaining gaps are mostly optimization/perf-layer specific.

Observed in tests:
- append/pop/insert/remove run correctly in interpreter for standard list cases.
- matrix transposition and matrix slicing paths remain stable.
- matrix method-style API for high-level kernels still deferred.

To remember:
- Keep matrix slice/view decisions phase-6 scope.
- When extending to performance core, prioritize canonical loop lowering before deeper container API.
- Re-run this log entry after each benchmark-focused refactor.

-------------------------------------------------------------------------------
15) PHASE SNAPSHOT UPDATE (2026-02-16)
-------------------------------------------------------------------------------
Phase 1:
- Done: benchmark harness + reproducibility gate + JSON/CSV outputs stabilized.
- Tried: strict perf/hyperfine pairing; fallback path kept for missing tools.
- Failed/lesson: ctest registration was incomplete; direct binary test execution is used.

Phase 2:
- Done: parser/AST/interpreter baseline stable for core subset.
- Tried: dual matrix literal syntax normalization to one AST.
- Failed/lesson: some syntax edge-cases surfaced only when codegen path was added.

Phase 3:
- Done: type/tier/shapes baseline and diagnostics path available.
- Tried: conservative widening/normalization to avoid unsafe fast-path assumptions.
- Failed/lesson: matrix `for` loop binding was initially scalar; fixed to row-list semantics.

Phase 4:
- Done: native IR->C->clang path works for scalar and selected container operations.
- Tried: codegen canonicalization + runtime helper expansion for list/matrix operations.
- Failed/lesson: runtime call kind inference had stale assumptions (`pop` return), fixed.

Phase 5:
- Done: typed list/matrix operations now connected end-to-end (parse/type/codegen/runtime).
- Tried: matrix slice combinations (`rows/cols/block`) and row-iteration lowering.
- Failed/lesson: method/runtime parity drifted temporarily (`pop/insert/remove` codegen vs runtime),
  now synchronized with tests and compile/run checks.
- New optimization pass:
  - C canonicalization no longer inlines direct call temporaries (prevents loop condition call duplication).
  - list append fast-path simplified (single capacity check branch).
  - matrix row extraction copy switched to `memcpy` for contiguous rows.
- Result: phase5 native/baseline geomean improved and current benchmark band gate (`0.85x-1.15x`) passes.
- Additional optimization pass (research-backed):
  - Codegen now detects canonical `while i < N` + `list.append(...)` loops and emits pre-loop reserve (`__spark_list_ensure_*`).
  - list shift-heavy ops (`pop/insert/remove`) switched from manual loops to `memmove`.
  - matrix row-slice copy path (`slice_rows`) switched to `memcpy`.
  - Added regression test for reserve lowering (`phase4 codegen tests`) to prevent silent rollback.
  - Stable benchmark run keeps baseline-band gate `2/2` with native/baseline geomean around `0.955x`.
- Tried and reverted in this window:
  - broad branch-hint/always-inline macro layering in runtime helpers reduced geomean (regression), reverted.
  - aggressive initial list growth (very high first capacity) increased overhead on mixed workloads, reverted.
- Additional optimization pass (toward >1.0x gate):
  - Added unchecked append lowering in reserve-proved loops (`__spark_list_append_unchecked_*`).
  - Added typed matrix constructors (`matrix_i64`, `matrix_f64`) in interpreter/typechecker/codegen.
  - Fixed `m[r,c]` index-chain order mismatch in both codegen and interpreter helpers.
  - Added focused forced-inline on list/matrix accessor helpers (not broad runtime macros).
  - Updated phase5 benchmark default native profile to `aggressive` and fixed `max` profile flags for clang compatibility.
- Result snapshot:
  - phase5 benchmark band gate: `2/2`
  - native vs baseline geomean: `1.035x` (latest stable gt1 artifact)
  - artifacts: `bench/results/phase5_benchmarks_gt1.json`, `bench/results/phase5_benchmarks_gt1.csv`
- Additional optimization/research pass (pass 4):
  - Investigated Clang/LLVM docs for alignment hints, PGO workflow, and vectorizer behavior.
  - Fixed PGO `use` compile breakage caused by accidental `-fcoverage-mapping` in use-mode flags.
  - Tried `native-profile=max` on phase5: regressed to ~`0.93x` geomean vs baseline (rejected).
  - Added benchmark-integrated auto-PGO pipeline (`instrument -> run -> merge -> use`) as optional mode.
  - Applied matrix-side fast-path improvements:
    - benchmark fill loops merged into single nested pass (`mat_a` + `mat_b`),
    - matrix contiguous data allocation moved to 64-byte aligned path,
    - matrix get/set helpers switched to aligned+restrict-friendly accesses.
  - Intermediate outcome: stable run reached
    - `list_iteration=1.019x`
    - `matrix_elemwise=1.052x`
    - geomean=`1.035x`
- Final stability trial (last run set before locking profile):
  - Compared `aggressive` vs `auto-native-pgo` with 3 runs each (same stable harness).
  - Aggregate results:
    - aggressive mean/stdev geomean: `0.980x` / `0.031`
    - auto-native-pgo mean/stdev geomean: `1.001x` / `0.015`
  - Decision:
    - lock Phase 5 benchmark default to auto-native-pgo path.
    - `bench/scripts/run_phase5_benchmarks.sh` now injects:
      - `--auto-native-pgo`
      - `--auto-native-pgo-runs 2`
  - Best confirmation run with locked default:
    - `list_iteration=1.0299x`
    - `matrix_elemwise=1.0495x`
    - geomean=`1.040x`
  - Follow-up stable run (same locked default):
    - `list_iteration=1.0067x`
    - `matrix_elemwise=0.9900x`
    - geomean=`0.998x`
  - Note:
    - single runs still fluctuate; profile lock is based on multi-run mean+variance, not one snapshot.

-------------------------------------------------------------------------------
16) OLUMLU / NEGATIF CALISMA KURALLARI (LOCKED)
-------------------------------------------------------------------------------
Olumlu (yapilacaklar):
- Karari tek run ile degil coklu run (mean + stdev + min) ile ver.
- Her optimizasyon adiminda once correctness gate (expected/test) gec.
- Benchmark algoritmasini C baseline ile birebir ayni tut.
- Degisiklikleri kucuk, izlenebilir ve geri alinabilir adimlarla yap.
- Her deneme sonunda sonuclari docs/txt loglarina yaz.
- Regresyon gorulurse hizla rollback et ve nedeni kaydet.
- Varsayilan profili sadece kararlilik kaniti varsa kilitle.

Negatif (yapilmayacaklar):
- Tek iyi sonuca bakip profile lock verme.
- Arastirma yapmadan agresif flagleri default acma.
- Ayni anda cok fazla degisken degistirip kok nedeni kaybetme.
- Baseline ile farkli algoritma/aksiyon kullanip haksiz kiyas yapma.
- Olcum gurultusunu (jitter) yok sayma.
- Dokumantasyon ve phase loglarini guncellemeden ilerleme.
- Buyuk monolitik script birikimine geri donme.

-------------------------------------------------------------------------------
17) PHASE 6 EXECUTION LOG (2026-02-17)
-------------------------------------------------------------------------------
Phase 6 target:
- Heterojen list/matrix calissin; hot-loop tarafinda analyze-plan-materialize-cache ile steady-state hiz artsin.

Yapilanlar:
- Value modeline LayoutTag + ListCache/MatrixCache eklendi:
  - PackedInt, PackedDouble, PromotedPackedDouble, ChunkedUnion, GatherScatter, BoxedAny.
- Runtime operasyonlari:
  - reduce_sum(), map_add(), plan_id(), cache_stats(), cache_bytes().
- Mutasyon invalidation:
  - append/pop/insert/remove + indexed set list/matrix.
- Phase6 benchmark hatti eklendi:
  - bench/scripts/run_phase6_benchmarks.py
  - bench/scripts/phase6/{definitions.py,utils.py}
  - bench/programs/phase6/*.k

Denenenler:
- Ilk denemede benchmark jitter yuksekti (kisa samplelar).
- sample_repeat eklendi (sample basina tekrar kosum + per-op normalize).
- Sonraki kosumlarda drift daha stabil hale geldi.

Basarili olanlar:
- Promote/Chunk/Gather planlari benchmarkta dogrulandi (plan ids: 3/4/5).
- First-run vs steady-state farki net goruldu (70x+ seviyesinde, normalize maliyeti bir kez odeniyor).
- Steady-state hetero/paked oranlari hedef bandda kaldi (<=1.8x).

Basarisiz/eksik kalanlar:
- perf stat Linux odakli; macOS ortaminda dogrudan yok.
- Matrix hetero tarafinda Chunk/Gather kernelleri hen uz phase kapsaminda degil (literal normalize + boxed fallback var).

Kayitli artefaktlar:
- bench/results/phase6_benchmarks.json
- bench/results/phase6_benchmarks.csv
- bench/results/phase6_benchmarks_comparisons.csv
- docs/phase6/containers_v2_semantics.md
- docs/phase6/phase6_report.md

-------------------------------------------------------------------------------
18) PHASE 6 RESEARCH + OPTIMIZATION LOG (2026-02-17, INTERNET PASS)
-------------------------------------------------------------------------------
Arastirma kaynaklari (primary):
- LLVM LangRef + Vectorizers
- V8 hidden-classes
- PyPy optimization notes
- Julia isbits union arrays
- Apache Arrow columnar format
- DuckDB vector internals
- CPython listobject.c (growth semantics)
- Weld paper (fusion + data movement)

Uygulanan teknik denemeler:
- live_plan fast-path:
  - Cache aktif degilken invalidate kisa devre.
  - Amaç: append agirlikli list insa asamasinda gereksiz invalidate maliyetini kesmek.
- ChunkedUnion numeric side-cache:
  - Materialize asamasinda numeric index/deger cache olustur.
  - reduce_sum/map_add tekrarinda per-element tag donusumunu azalt.

Denenip geri alinan:
- PackedInt/PackedDouble icin ek packed buffer'lari dogrudan ListCache icinde tutma:
  - Value boyutu buyudugu icin genel list elemani maliyeti artti.
  - Sonuc: bu yaklasim geri alindi; out-of-line tasarim olmadan tekrar uygulanmayacak.

Son olcum snapshot (phase6, runs=7, sample_repeat=3):
- packed steady: ~759546.30 ns/op
- promote steady: ~849283.09 ns/op (steady/packed ~1.118x)
- chunk steady: ~935912.89 ns/op (steady/packed ~1.232x)
- gather steady: ~1003318.07 ns/op (steady/packed ~1.321x)
- first->steady hizlanma tum hetero gruplarda ~74x-79x bandinda

Bu pass'ten cikan kurallar:
Olumlu:
- Hot loop'ta tek dispatch: analyze-plan kararini loop disinda kilitle.
- Hetero numeric icin bir kez normalize/promote et, steady-state'te unboxed calistir.
- Plan secimini erisim desenine gore yap (iter-heavy=chunk, order-kritik numeric=gather).
- Kararlilik karari icin tek run degil coklu-run istatistiklerini kullan.

Negatif:
- Value gibi her elemana tasinan yapilari metadata ile sisirme.
- Semantik degistiren agresif numerik bayraklari default acma.
- Single-run sonucuna gore profil/plan lock etme.

Ilgili dokuman:
- docs/phase6/phase6_speedup_rules.md

-------------------------------------------------------------------------------
19) PHASE 7 EXECUTION LOG (2026-02-17)
-------------------------------------------------------------------------------
Phase 7 target:
- map/filter/zip/reduce/scan pipeline zincirlerini fused calistirip intermediate allocation'i azaltmak.

Yapilanlar:
- Yeni runtime modulleri eklendi:
  - compiler/src/phase7/runtime/01_pipeline_types.cpp
  - compiler/src/phase7/runtime/02_pipeline_parse.cpp
  - compiler/src/phase7/runtime/03_pipeline_exec_list.cpp
  - compiler/src/phase7/runtime/04_pipeline_exec_matrix.cpp
  - compiler/src/phase7/runtime/05_pipeline_entry.cpp
- evaluator call path pipeline chain'i direkt yakalayip:
  - fused path (SPARK_PIPELINE_FUSION=1)
  - non-fused fallback (SPARK_PIPELINE_FUSION=0) seklinde calisiyor.
- Runtime gozlemlenebilirlik:
  - pipeline_stats()
  - pipeline_plan_id()
- Analyze diagnostics:
  - --dump-pipeline-ir
  - --dump-fusion-plan
  - --why-not-fused

Testler:
- sparkc_smoke_test
- sparkc_parser_tests
- sparkc_eval_tests
- sparkc_typecheck_tests
- sparkc_codegen_tests
- sparkc_phase5_tests
- sparkc_phase6_tests
- sparkc_phase7_tests

Phase7 benchmark hatti:
- bench/scripts/phase7/definitions.py
- bench/scripts/phase7/utils.py
- bench/scripts/run_phase7_benchmarks.py
- bench/scripts/run_phase7_benchmarks.sh
- bench/programs/phase7/*.k

Son olcum snapshot (runs=5, warmup=1):
- list_pipeline_reduce: 2.664x (fused vs non-fused)
- hetero_pipeline_reduce: 2.017x
- matrix_pipeline_reduce: 3.576x

Kayitli artefaktlar:
- bench/results/phase7_benchmarks.json
- bench/results/phase7_benchmarks.csv
- docs/phase7/pipeline_semantics.md
- docs/phase7/phase7_report.md

Denenen ve duzeltilen:
- Matrix non-fused pipeline path'te stage tekrar uygulama hatasi vardi (checksum kayiyordu).
  - Duzeltme: non-fused matrix stage uygulamasi adim-bazli hale getirildi.
- Type inference tarafinda [int, int, float] literalin List[Any]'e dusmesi goruldu.
  - Duzeltme: numeric widen durumunda Any'e dusme kaldirildi.

-------------------------------------------------------------------------------
20) PHASE 7 RESEARCH NOTES (SHORT)
-------------------------------------------------------------------------------
Internet arastirmasindan alinip uygulanan prensipler:
- Deforestation/loop fusion: ara container'lari kaldir.
- Materialization policy explicit olsun: her zincir fuse edilmez, nedenini raporla.
- Steady-state odakli olcum: first-run ve tekrar kosum farkini ayri izle.
- Chunk/gather gibi hetero path'lerde hot loop'u temiz tut, tag/dispatch'i loop disina tasit.

Olumlu (yap):
- Pipeline kararini analyze->plan->execute seklinde tek yerde tut.
- Fused ve non-fused yollari ayni checksum ile dogrula.
- Runtime sayaçlarini benchmark formatina dogrudan bagla (alloc, plan, fused/fallback).

Negatif (yapma):
- Zincirde mutasyon/rastgele erisim varken zorla fusion.
- Tek bir benchmark sonucuna bakip optimizasyonu kilitleme.
- Diagnose edilemeyen implicit materialization'a izin verme.

-------------------------------------------------------------------------------
21) PHASE 7 OPTIMIZATION PASS LOG (2026-02-17, LATE PASS)
-------------------------------------------------------------------------------
Problem:
- Fused path'te receiver list/matrix degeri kopyalaniyordu; buyuk veride gereksiz maliyet olusturuyordu.

Uygulanan degisiklikler:
- execute_list_pipeline / execute_matrix_pipeline:
  - receiver kopyasi kaldirildi, const-reference ile okunuyor.
- PackedInt reduce_sum icin dogrudan integer fast path eklendi.
- Kisa transform zincirleri icin inner-loop dispatch sadeleştirildi.

Son olcum (phase7 benchmark, runs=5):
- list_pipeline_reduce: 3.216x
- hetero_pipeline_reduce: 2.405x
- matrix_pipeline_reduce: 3.627x

Not:
- Onceki snapshot'a gore tum phase7 karsilastirmalari iyilesti.

-------------------------------------------------------------------------------
22) PHASE 8 EXECUTION LOG (2026-02-17)
-------------------------------------------------------------------------------
Phase 8 target:
- Matmul kernel/schedule/pack-cache + epilogue fusion + minimal autotuning.

Yapilanlar:
- Runtime modulleri:
  - compiler/src/phase8/runtime/01_schedule.cpp
  - compiler/src/phase8/runtime/02_pack_cache.cpp
  - compiler/src/phase8/runtime/03_matmul_kernel.cpp
  - compiler/src/phase8/runtime/04_matmul_entry.cpp
- Language/API:
  - matmul, matmul_f32, matmul_f64
  - matmul_add, matmul_axpby
  - matmul_stats, matmul_schedule
- Hybrid backend:
  - own tiled kernel + BLAS (cblas_dgemm/sgemm) secimi
- Pack/cache:
  - A/B packed cache + probe tabanli stale kontrol
- Tuning:
  - tune/matmul_tuner.py
  - output: bench/results/matmul_tuned_schedule.json

Test:
- sparkc_phase8_tests eklendi ve PASS.

Benchmark:
- bench/scripts/run_phase8_benchmarks.py
- output:
  - bench/results/phase8_benchmarks.json
  - bench/results/phase8_benchmarks.csv
- latest comparison:
  - matmul_core_f64: own/blas ~0.973x, auto/best ~1.085x
  - matmul_epilogue_f64: own/blas ~0.924x, auto/best ~1.150x
  - matmul_core_f64_256: own/blas ~1.190x, auto/best ~0.984x

-------------------------------------------------------------------------------
24) PHASE 8 SPEEDUP PASS LOG (2026-02-17, FOLLOW-UP)
-------------------------------------------------------------------------------
Problem:
- Phase8 benchmarkte wall-time'in buyuk kismi matmul kernelden degil, interpreted setup
  (matrix doldurma + expected checksum loop'lari) tarafindan harcaniyordu.

Uygulanan degisiklikler:
- Schedule path:
  - tuned schedule dosya parse islemi call-basi yerine cache'lendi (config degisirse reload).
  - auto backend BLAS gecis esigi 128^3 problem boyutuna cekildi.
- Matrix plan/cache:
  - matrix plan cache tekrar kullanimi operation string'e bagli olmaktan cikarildi.
  - matmul sonucu matrix'lerde Packed plan metadata dogrudan set edilerek reduce_sum analiz maliyeti kesildi.
- Runtime utility builtins:
  - matrix_fill_affine(...)
  - matmul_expected_sum(lhs, rhs)
- Phase8 benchmark programlari bu builtins'lere tasindi.

Etki (son run):
- matmul_core_f64 own median wall-time: ~0.105s -> ~0.015s (yaklasik 6.9x iyilesme)
- phase8 benchmark gate: records 12/12 PASS, comparisons 3/3 PASS

Ders:
- Kernel odakli olcum icin orchestration maliyetini runtime utility katmanina tasimak gerekiyor.
- Auto backend secimi latency/throughput dengesinde buyuk fark yaratiyor.

-------------------------------------------------------------------------------
23) PHASE 8 OLUMLU / NEGATIF KURALLAR (LOCKED)
-------------------------------------------------------------------------------
Olumlu (yap):
- Schedule'i semantikten ayir; yalniz performans karari tasisin.
- BLAS varsa hybrid path ile hizli kazanimi acik tut.
- Pack/cache hit oranlarini runtime'da gorunur tut (stats API).
- Epilogue islemlerini tek planda calistirarak extra pass'i azalt.
- Tuning sonucunu JSON cache ile tekrar kullan.

Negatif (yapma):
- Fast-math'i default acip dogrulugu riske atma.
- Cache key'ini probe/versiyon olmadan kurup stale pack kullanma.
- Tek backend'e kilitlenip fallback'i kaybetme.
- Benchmarkta interpreter orchestration maliyetini kernel speed ile karistirip yanlis karar verme.

-------------------------------------------------------------------------------
25) PHASE 7-8 STABILITY PASS LOG (2026-02-17, FINAL)
-------------------------------------------------------------------------------
Hedef:
- Phase 7 ve Phase 8'de en kararlı hiz profiline gecmek ve bu profile gore devam etmek.

Yapilan degisiklikler:
- Phase 7:
  - list/matrix fused runtime'da `map_mul -> reduce_sum` icin ek fast path.
- Phase 8:
  - own GEMM icinde `b_transposed` kontrolu inner loop disina tasindi (branch hoist).
  - epilogue (bias/axpby) tarafinda shape-check/dispatch tek sefer hazirlanip
    hucre-basi erisim sadeleştirildi.
  - auto backend karari buyuk matrislerde BLAS secimini koruyacak sekilde
    dim+volume esiklerine baglandi:
      - `SPARK_MATMUL_AUTO_DIM_THRESHOLD` (default 224)
      - `SPARK_MATMUL_AUTO_VOLUME_THRESHOLD` (default 224^3)
  - Tuned backend kilitlemesi varsayilan olarak kapali tutuldu;
    gerekirse `SPARK_MATMUL_AUTO_RESPECT_TUNED_BACKEND=1` ile acilabilir.

Son stable olcum (runs=5, warmup=1, sample_repeat=3):
- Phase 7:
  - list_pipeline_reduce: 3.552x
  - hetero_pipeline_reduce: 1.823x
  - matrix_pipeline_reduce: 3.553x
- Phase 8 comparisons:
  - matmul_core_f64: own/blas 0.943x, auto backend own
  - matmul_epilogue_f64: own/blas 0.958x, auto backend own
  - matmul_core_f64_256: own/blas 1.140x, auto backend blas

Karar (lock):
- Kucuk/orta boyutlarda own backend, buyuk matrislerde auto->BLAS stratejisi ile devam.
- Benchmark stabilitesi icin `sample_repeat=3` korunacak.

-------------------------------------------------------------------------------
26) PHASE 8 BENCH EXTENSION (N=512) + SPEED TRIAL (2026-02-17)
-------------------------------------------------------------------------------
Yapilanlar:
- Phase8 benchmark setine `matmul_core_f64_512` eklendi:
  - bench/programs/phase8/matmul_core_f64_512.k
  - bench/programs/phase8/c_baselines/matmul_core_f64_512.c
  - bench/scripts/phase8/definitions.py guncellendi.
- 512 case dogruluk toleransi:
  - k program ve C baseline icin `diff < 1e-4`.
  - Sebep: toplam buyuklugunde floating birikim farki (`~1.97e-5`) deterministic ama 1e-5'i asiyor.

Tum testler:
- sparkc_smoke_test, parser/eval/codegen/typecheck, phase5/6/7/8 testleri PASS.

Benchmark sonucu (runs=5, warmup=1, sample_repeat=3):
- phase8 records: 16/16 PASS
- phase8 comparisons: 4/4 PASS
- matmul_core_f64_512:
  - own median: ~0.350s
  - auto median: ~0.242s (backend=BLAS)
  - blas median: ~0.243s
  - own_vs_blas: ~1.439x
  - auto_vs_best: ~0.995x

Hizlandirma denemesi (olumsuz):
- Buyuk own-kernel icin tile>=128 zorlama denendi.
- Sonuc: 512 own path yavasladi (daha kotu median).
- Karar: geri alindi, mevcut tuned/auto stratejisi korundu.

-------------------------------------------------------------------------------
27) PHASE 7-8 DEEP RESEARCH + TARGETED SPEED PASS (128/256/512)
-------------------------------------------------------------------------------
Arastirma ozeti (uygulanan):
- Goto/BLIS prensibi: cache-aware blocking + mikro-kernel register birikimi.
- LLVM vectorizer notlari: inner-loop branch ve dispatch azaltimi.
- Weld/deforestation mantigi: pipeline terminalinde gereksiz ara islem azaltimi.

Uygulananlar:
- Phase 7:
  - reduce_sum terminalinde map_add/map_mul zincirleri affine formulle
    sadeleştirildi (tek toplam + sabit duzeltme).
- Phase 8:
  - own GEMM transposed-B yoluna 4-kolon mikro-kernel eklendi (n>=192).
  - matrix_fill_affine icin large matrixlerde (>=128^2) eager dense f64 cache.
  - acquire_packed_matrix non-transpose f64 yolunda hazir dense cache dogrudan kullanildi.
  - matmul result matrix'te gereksiz dense cache kopyasi kaldirildi.

Denenen ama geri alinan:
- buyuk own-kernel icin tile>=128 zorlama (regresyon verdi, geri alindi).

Son benchmark (phase8, runs=5, warmup=1, sample_repeat=3):
- records: 16/16 PASS
- comparisons: 4/4 PASS
- 128 (matmul_core_f64):
  - own median ~0.0578s
  - auto median ~0.0707s
- 256 (matmul_core_f64_256):
  - own median ~0.1200s
  - auto median ~0.1061s (auto->BLAS)
- 512 (matmul_core_f64_512):
  - own median ~0.2946s
  - auto median ~0.2188s (auto->BLAS)

Not:
- 512 için onceki stabil snapshot'a gore own ve auto pathte belirgin iyilesme goruldu.
- 256/512'de "en kararlı hiz" icin auto->BLAS stratejisi korunuyor.

-------------------------------------------------------------------------------
28) PHASE 8 STABILITY HOTFIX + ADAPTIVE TILE PASS (2026-02-17)
-------------------------------------------------------------------------------
Problem:
- Phase8 optimize denemesi sonrasi `matmul_epilogue_f64` case'inde
  `auto_vs_best` gecici olarak bozuldu (4/4 yerine 3/4).
- Kucuk problemde auto backend yolunda gereksiz BLAS probe latency gorduk.

Yapilanlar:
- `resolve_schedule` icinde auto backend karari:
  - `has_blas_backend() && large_problem` yerine
  - `large_problem && has_blas_backend()` olarak duzeltildi.
  - Boylece small-size path BLAS probe'a zorlanmiyor.
- Boyut-adaptif tile kurali eklendi:
  - f64 max_dim <= 160 -> tile 96
  - f64 max_dim >= 224 -> tile 64
- transposed-B own kernelde n>=256 icin 8-kolon mikro-kernel eklendi.

Sonuc:
- Phase8 benchmark: records 16/16 PASS, comparisons 4/4 PASS.
- Medyan sureler (latest):
  - 128 core: own ~0.0413s, auto ~0.0419s, blas ~0.0446s
  - 256 core: own ~0.0842s, auto ~0.0729s, blas ~0.0726s
  - 512 core: own ~0.2071s, auto ~0.1562s, blas ~0.1607s
- Phase7 benchmark da stabil:
  - list 3.874x, hetero 2.284x, matrix 3.604x.

Ders:
- Tek tuned tile tum boyutlar icin optimum degil; size-banded tile daha stabil.
- Auto backend latency'sinde karar sirası bile kritik fark olusturuyor.

-------------------------------------------------------------------------------
29) PHASE 9 CONCURRENCY BASELINE (2026-02-17)
-------------------------------------------------------------------------------
Yapilanlar:
- Frontend:
  - `async fn` alias destegi eklendi (`async def` ile ayni AST'ye normalize).
  - `await` unary parse/eval aktif.
  - `with task_group([timeout_ms]) as g:` parse/eval aktif.
- Runtime:
  - work-stealing scheduler (submit/execute/steal stats),
  - task API: `spawn`, `join`, `cancel`, `task_group`,
  - parallel API: `parallel_for`, `par_map`, `par_reduce`,
  - event API: `channel/send/recv/close`,
  - stream API: `stream`, `anext`, `channel.anext()`, `channel.has_next()`.
- Static diagnostics:
  - `spawn/par_*` icin named-function sendable kontrolu,
  - non-sendable capture argumanlar icin compile-time hata/rapor.
- Test paketi:
  - `tests/phase9/runtime/*` (async/task/channel/parallel)
  - `tests/phase9/analyze/*` (safety diagnostics)
- Benchmark paketi:
  - `bench/scripts/run_phase9_benchmarks.py`
  - spawn/join overhead, channel throughput+latency,
    parallel_for scaling, par_reduce chunk tuning.

Denenen ama uygulanmayan:
- Tam MIR/SSA async state-machine lowering:
  - bu sprintte stabil runtime + olcum hattina odaklanildi,
    compiler-level lowering sonraki adima birakildi.
- Agresif lock-free channel denemesi:
  - dogruluk/backpressure karmaşıkligi nedeniyle v1'de mutex+cv korundu.

Olumlu:
- Structured concurrency scope semantigi testlenebilir hale geldi.
- Event-driven pipeline primitive seti (channel+stream) tek syntaxla calisiyor.
- Scheduler/perf metrikleri JSON/CSV hattina alinabildi.

Negatif / Dikkat:
- Full ownership/borrow guvencesi yok; su an conservative diagnostic seviyesi.
- `parallel_for` path'te closure-capture tipi kotu secilirse T4 fast path reddediliyor.

-------------------------------------------------------------------------------
30) PHASE 9 PERF TUNING (2026-02-17, follow-up)
-------------------------------------------------------------------------------
Problem tespiti:
- Ilk phase9 bench turunda:
  - spawn/join overhead yuksekti,
  - parallel_for scaling olcumu channel lock contention ile kirleniyordu,
  - channel benchmarkta bir deadlock vakasi goruldu (bounded queue + gec baslayan consumer).

Yapilan optimizasyonlar:
- Scheduler:
  - poll-based bekleme kaldirildi,
  - pending-task sinyali ile cv-wake modeli eklendi.
- Runtime call hot path:
  - parallel_for/par_map/par_reduce icinde per-iteration arg vector allocation azaltildi.
- Closure modeli:
  - concurrent call path icin frozen closure env kullanildi
    (paylasilan mutable parent env contention/race riski azaltildi).
- Benchmark duzeltmeleri:
  - parallel_for scaling programi chunk-lock contention'dan arindirildi
    (gercek scheduler scaling gorunur hale getirildi).

Denenen ama geri alinan / degistirilen:
- parallel_for scaling icin channel'a her elemanda send:
  - scaling'i yapay olarak bastirdi (lock contention), olcum modeli degistirildi.
- channel queue'da buyuk `Value` kopyasi:
  - throughput geriledi; pointer-backed queue'ya donuldu.

Son stable phase9 metrikleri:
- spawn/join: ~49.3k ns/task
- channel throughput: ~351k msg/s, latency ~2.85 us/msg
- parallel_for speedup:
  - 2 thread: ~1.96x
  - 4 thread: ~3.03x
  - 8 thread: ~4.09x
- par_reduce chunk best: 128

Karsilastirma (phase9 baseline'a gore):
- spawn/join overhead: ~%95 iyilesme
- parallel max speedup: ~3.88x -> ~4.09x

-------------------------------------------------------------------------------
31) PHASE 9 GAP-CLOSURE PASS (2026-02-17, final)
-------------------------------------------------------------------------------
Yapilanlar:
- `async for` syntax + runtime eklendi:
  - parser: `async for v in stream(ch): ...`
  - evaluator: channel/stream drain ile async-iter style dongu.
- Analyzer dump eklendi:
  - `sparkc analyze file.k --dump-async-sm`
  - async function icin await noktasi + state sayisi raporu.
- Concurrency safety sertlestirmesi:
  - spawn/par_* capture argumanlarinda non-sendable tipleri compile-time reject.
  - concurrent function call path icin frozen closure env ile mutabilite izolasyonu.

Test/Validation:
- parser tests PASS
- phase9 tests PASS
- phase9 benchmark sections: 4/4 PASS

Not:
- Async state-machine raporu su an pseudo-lowering (analyzer seviyesinde)
  ve runtime task frame'i ile hizalanmis durumda.

-------------------------------------------------------------------------------
32) PHASE 9 DEADLINE + SNAPSHOT SYNC (2026-02-17)
-------------------------------------------------------------------------------
Yapilanlar:
- Timeout/deadline beklentisini netlemek icin `deadline(ms)` alias builtin eklendi.
  - Runtime: non-negative kontrol + int timeout token donus.
  - Semantic: `deadline(int) -> int` builtin type tanimi.
  - Docs: grammar + runtime semantigi + decisions guncellendi.
- Test kapsamina yeni senaryolar eklendi:
  - `join(task, deadline(...))` dogrulama testi.
  - `recv(ch, 0)` timeout hata testi (background task sızıntısı olmadan stabil).
- Analyzer davranisi runtime ile hizalandi:
  - `with task_group(...) as g` baglami scope disinda da gorunur (Python-style).
- Phase9 raporu son benchmark JSON verileriyle senkronize edildi.

Son stable phase9 metrikleri (latest rerun):
- spawn/join: ~90.3k ns/task
- channel throughput: ~160k msg/s, latency ~6.23 us/msg
- parallel_for speedup:
  - 2 thread: ~1.87x
  - 4 thread: ~2.40x
  - 8 thread: ~3.96x
- par_reduce chunk best: 64

Ders:
- Bu snapshot'ta thread scaling iyilesirken channel throughput onceki snapshot'a gore dustu;
  scheduler/queue ayarlarinin workload bagimli oldugu tekrar dogrulandi.

-------------------------------------------------------------------------------
33) PHASE 9 TAVAN ARAYISI (2026-02-17, runtime fast-path)
-------------------------------------------------------------------------------
Yapilanlar:
- Scheduler submit path:
  - `packaged_task` yerine promise tabanli submit yolu eklendi.
  - parallel primitive'ler icin `scheduler_submit_fire_and_forget()` eklendi
    (future/get overhead'i kaldirildi).
- Await/join assist:
  - `await_task_value()` beklerken scheduler'dan pending task alip calistirabiliyor.
  - Ozellikle `spawn -> join` pattern'inde bos bekleme yerine faydali is yapiliyor.
- Parallel runtime:
  - `parallel_for/par_map/par_reduce` icin wait-group + exception propagation modeli eklendi.
  - chunk baseline yeniden sabitlendi: `<=4096 -> 64` (env override korunuyor).
- Channel runtime:
  - notify optimizasyonu: sadece queue state transition oldugunda wake
    (`empty->non-empty`, `full->non-full`).

Denenen ama geri alinan:
- Thread-sayisina gore adaptif chunk heuristigi:
  - bazi kosularda tek-run hizli olsa da tekrar kosularda dalgalanma artirdi.
  - "en kararlı" hedefi icin fixed-64 default'a geri donuldu.

Olumlu (yap):
- wait-group + fire-and-forget ile parallel chunk path'te future/get maliyetini azalt.
- join/await beklerken scheduler assist kullanarak idle kalma.
- channel tarafinda gereksiz notify'i azalt.

Negatif (yapma):
- chunk politikasini surekli heuristiklerle degistirip kararliligi bozma.
- benchmarki parallel kosup (phase7/8/9 ayni anda) snapshot karari verme.
- channel'da tum queue eventlerinde notify_all kullanma.

Son durum (final config ile tekrarli olcum):
- phase9 sections: 4/4 PASS
- 3-run median:
  - spawn/join: ~30.7k .. ~44.8k ns/task
  - channel throughput: ~213k .. ~315k msg/s (latency ~3.17 .. ~4.70 us/msg)
  - parallel_for speedup: 2T ~1.79 .. ~1.84x, 4T ~2.91 .. ~3.14x, 8T ~4.36 .. ~5.18x
  - par_reduce best chunk: 64..256 bandi

Not:
- Tekil run'larda OS scheduling kaynakli dalgalanma var; kararlar median snapshot'a gore alindi.

-------------------------------------------------------------------------------
34) PHASE 10 UYGULAMA NOTLARI (2026-02-17)
-------------------------------------------------------------------------------
Yapilanlar:
- Multi-arch build kapisi eklendi:
  - `scripts/phase10/multiarch_build.py`
  - hedefler: `x86_64-linux-gnu`, `aarch64-linux-gnu`, `riscv64-linux-gnu`
  - JSON artifact: `bench/results/phase10_multiarch.json`
- CPU dispatch gozlemlenebilirligi:
  - `sparkc --print-cpu-features`
  - dispatch test override: `SPARK_CPU_ARCH`, `SPARK_CPU_FEATURES`
- Final perf pipeline:
  - `scripts/pgo_cycle.sh` (instrument -> merge -> use)
  - `scripts/bolt_opt.sh` (perf/perf2bolt/llvm-bolt varsa)
- Safety gates:
  - differential: `scripts/phase10/differential_check.py`
  - parser fuzz: `scripts/phase10/fuzz_parser.py`
  - runtime fuzz: `scripts/phase10/fuzz_runtime.py`
  - sanitizer: `scripts/phase10/run_sanitizers.py`
  - toplu: `scripts/phase10_safety_gates.sh`
- Phase10 benchmark orchestrator:
  - `bench/scripts/run_phase10_benchmarks.py`
  - JSON/CSV/MD phase10 raporu uretiyor.

Olumlu (yap):
- PGO cikislarini mutlaka medyan sure ile kaydet (`baseline vs pgo` ayni program).
- Dispatch dogrulamasini ayni programda farkli forced feature setleriyle yap.
- Cross-target build sonuclarini PASS/FAIL + log ile artifact'a yaz.
- Tool yoksa (BOLT gibi) script'i kirmadan "skipped + reason" raporla.

Negatif (yapma):
- Multi-arch sonucu "toolchain yoksa sessizce gecme"; acik skip/fail yaz.
- Fast-math'i varsayilan acma; strict sonucu bozan hiz kazanci kabul etme.
- Differential/fuzz scriptlerinde stderr diagnostiklerini normalize etmeden direkt string karsilastirma.
- Tek run verisiyle PGO/BOLT karari alma; medyan ve drift bilgisi tut.

Sonuc:
- Phase10 kapsaminda portability + dispatch + final optimization + safety gating zinciri
  scriptlenmis ve artifact-ureten hale getirildi.

-------------------------------------------------------------------------------
35) FAST FOLLOW HIZLANDIRMA NOTLARI (2026-02-17)
-------------------------------------------------------------------------------
Yapilanlar:
- Phase8 own GEMM icin row-tile parallel path eklendi:
  - kod: `compiler/src/phase8/runtime/03_matmul_kernel.cpp`
  - yeni env ayarlari:
    - `SPARK_MATMUL_OWN_THREADS`
    - `SPARK_MATMUL_OWN_PAR_MIN_VOLUME`
- Phase10 PGO hattina no-regression secimi eklendi:
  - kod: `scripts/pgo_cycle.sh`
  - `raw_speedup_vs_baseline` ve `selected_variant` artifact'a yaziliyor.
  - raw PGO daha kotu cikarsa etkili secim baseline'a donuyor.

Denenen ama geri alinan:
- PGO profile workload'unu sadece buyutmek (`N=5_000_000`) kararliligi artirmadi;
  dalgalanma devam etti. Kalici degisiklik olarak alinmadi.

Olumlu (yap):
- Matmul'da disjoint satir-tile paralelligi kullan (deterministik + race-free).
- PGO kararini raw sonuca gore otomatik sec (pgo_use/baseline) ve artifact'a yaz.
- Karari tek kosu degil, tekrarli benchmark + medyan ile ver.

Negatif (yapma):
- Kisa sureli tek-run hiz artisina bakip kalici karar verme.
- PGO yavaslatirken release-perf'e zorla pgo binary surme.
- Matmul icte race riski olan reduction-paralelligi deneme (row disjoint garanti disina cikma).

Son durum:
- Phase8 benchmark: `16/16 records PASS`, `4/4 comparisons PASS`.
- Phase10 benchmark: `4/4 sections PASS` (PGO no-regression secimi aktif).
- Kontrollu own-GEMM A/B (9 tekrar medyan):
  - 256: `threads=1 -> ~0.02939s`, `threads=4 -> ~0.02450s` (`~1.20x`)
  - 512: `threads=1 -> ~0.07498s`, `threads=4 -> ~0.05377s` (`~1.39x`)

-------------------------------------------------------------------------------
36) PHASE 10 EK HIZLANDIRMA (2026-02-17, AutoFDO + ISA dispatch + BOLT)
-------------------------------------------------------------------------------
Yapilanlar:
- AutoFDO hattı eklendi:
  - `scripts/autofdo_cycle.sh`
  - akış: `perf record` -> `llvm-profgen` -> `-fprofile-sample-use`
  - PGO ile aynı no-regression modeli: raw yavaşsa baseline seçiliyor.
- Phase10 benchmark orchestrator genisletildi:
  - `bench/scripts/run_phase10_benchmarks.py`
  - yeni `autofdo` section + `min_autofdo_speedup` gate
  - rapor formatı `5/5` section modeline geçirildi.
- BOLT script iyilestirildi:
  - `scripts/bolt_opt.sh`
  - `--profile-runs` ile daha stabil profiling
  - raw/effective speed + selected variant artifact alanlari eklendi.
- ISA dispatch tabanlı kernel tuning eklendi:
  - `compiler/src/phase8/runtime/03_matmul_kernel.cpp`
  - varyant bazlı (`x86_avx2/avx512`, `arm_neon/sve/sve2`, `riscv_rvv`)
    - 8-col / 4-col mikro-kernel giris esikleri
    - paralel thread cap ve min-volume defaultlari
  - `arm_neon` için kalıcı profil: `j8=max`, `j4=128`, `thread_cap=12`
  - runtime tuning override env'leri:
    - `SPARK_MATMUL_OWN_J8_THRESHOLD`
    - `SPARK_MATMUL_OWN_J4_THRESHOLD`
    - `SPARK_MATMUL_OWN_DEFAULT_THREADS`
    - `SPARK_MATMUL_OWN_DEFAULT_MIN_VOLUME`

Denenen ama geri alinan:
- AutoFDO için sadece profile workload büyütme yaklaşımı önceki denemede stabil fayda üretmedi;
  tek başına çözüm olarak alınmadı.

Olumlu (yap):
- AutoFDO/BOLT yoksa build’i kırmadan artifact’a `skipped + reason` yaz.
- Dispatch tuning’i CPU feature tag ile seç ve env override açık bırak.
- Release-perf kararı için raw ve effective speed’i ayrı kaydet.

Negatif (yapma):
- Toolchain eksik hostta AutoFDO/BOLT zorlayıp hatayla pipeline’ı durdurma.
- Sadece tek benchmark örneğine göre tüm profile kararını kilitleme.
- ISA varyantlarını semantiği değiştirecek şekilde farklılaştırma.

Son durum:
- Phase10 benchmark: `5/5 sections PASS`
  - PGO: PASS (no-regression secimi; host kosusuna gore `pgo_use` veya `baseline`)
  - AutoFDO: skip-support PASS
  - BOLT: skip-support PASS

Yeni deneme (matmul sonuc/fark + hiz):
- 100x100 tekrarli workload'da (`repeats=100`) `auto` backend bu hostta own seciyordu ve BLAS'a gore yavas kaldi.
- Auto policy guncellendi:
  - `compiler/src/phase8/runtime/01_schedule.cpp`
  - yeni env: `SPARK_MATMUL_AUTO_SMALL_BLAS_DIM_THRESHOLD` (default `112`)
  - auto secim: cok kucuk boyutta da BLAS yolu aktif.
- Strict FP kontrolu eklendi:
  - `compiler/src/phase8/runtime/03_matmul_kernel.cpp`
  - `SPARK_MATMUL_STRICT_FP=1` ile deterministic toplama sirasi (dogrulama modu).
- Not:
  - strict FP sonucu yaklastirir ama hiz dusurur.
  - performans modu icin strict kapali tutulmali; bu mod sadece parity/debug icin.

Numerik fark azaltma (hiz koruma):
- Yeni builtin: `accumulate_sum(total, list_or_matrix)`
  - dosya: `compiler/src/phase5/runtime/interpreter_core.cpp`
  - amac: C tarafindaki `total += c[i][j]` akisina denk birikimi tek builtin ile yapmak.
  - `reduce_sum` yerine bunu kullaninca runlar arasi grouping farki azalir, fark 1e-12/1e-13 hedefe yaklasir.
  - interpreter index loop maliyeti olmadan (tek builtin cagrisi) calistigi icin hiz korunur.
- 100x100x100 tekrar testinde:
  - eski akıs (`total += c.reduce_sum()`): fark ~`7.218e-05`
  - yeni akıs (`total = accumulate_sum(total, c)`): C/Java/Python ile ayni double sonuc (`abs diff = 0.0`)
  - medyan sure: eski `~0.02864s`, yeni `~0.02770s` (hiz kaybi yok, hafif iyilesme)

-------------------------------------------------------------------------------
37) MATRIX '*' OPERATORU + LOOP HOT-PATH (2026-02-17)
-------------------------------------------------------------------------------
Yapilanlar:
- `matrix * matrix` davranisi runtime'da matmul yoluna sabitlendi:
  - dosya: `compiler/src/phase5/runtime/interpreter_ops.cpp`
  - kontrol: `lhs.cols == rhs.rows`, uyusmazlikta acik hata.
- Type/shape analiz matmul semantigine hizalandi:
  - dosyalar:
    - `compiler/src/phase3/semantic_parts/04_analysis.cpp`
    - `compiler/src/phase3/semantic_parts/05_expr_infer.cpp`
  - `matrix '*' lowers to matmul kernel path when eligible` reason korunuyor.
- `for/while` interpreter hot-path iyilestirmeleri:
  - `compiler/src/phase3/evaluator_parts/stmt/stmt_while.cpp`
    - empty-body ve single-stmt fast path.
  - `compiler/src/phase3/evaluator_parts/stmt/stmt_for.cpp`
    - loop variable slot pointer cache (`get_ptr` bir kez),
    - body dispatch fast path (empty/single),
    - matrix iterasyonunda row-list buffer reuse (per-iter allocation azaltimi),
    - `cols == 1` icin scalar fast path korunuyor.
- Matrix arithmetic memory/branch hot-path:
  - `compiler/src/phase5/runtime/interpreter_ops.cpp`
  - out buffer pre-sized write,
  - operation branch'i loop disina tasindi (add/sub/mul/div ayrik looplar).

Test/Doğrulama:
- Derleme:
  - `cmake --build build -j8` PASS
- Testler:
  - `sparkc_smoke_test`, `sparkc_parser_tests`, `sparkc_eval_tests`, `sparkc_typecheck_tests`,
    `sparkc_codegen_tests`, `sparkc_phase5_tests`, `sparkc_phase6_tests`,
    `sparkc_phase7_tests`, `sparkc_phase8_tests`, `sparkc_phase9_tests`, `sparkc_phase10_tests`
  - hepsi PASS.
- Yeni/guclendirilen testler:
  - `tests/phase5/matrix/matrix_container_tests.cpp`:
    - `test_matrix_star_is_matmul` (2x3 * 3x2 => 2x2 dogrulama).
  - `tests/phase3/typecheck_inference_shape_tests.cpp`:
    - non-square matmul shape inference + shape mismatch error testi.
- Loop hiz snapshot'u (`/usr/bin/time`, 9 run, medyan):
  - not: bu hostta `hyperfine` yok, fallback olarak `/usr/bin/time` kullanildi.
  - `./k run bench/programs/phase4/scalar_sum.k` => `~0.40s`
  - `./k run bench/programs/phase4/scalar_for_range_mul.k` => `~0.39s`
  - `./k run bench/programs/phase5/list_iteration.k` => `~0.36s`

Olumlu (yap):
- Matrix operator semantigini tek yerde netlestir (`*` => matmul for matrix-matrix).
- Loop fast-path'lerde semantik degismeden allocation ve dispatch azalt.
- Her semantic degisiklikte dokuman + testi birlikte guncelle.

Negatif (yapma):
- Matrix-matrix `*` icin elementwise fallback'e sessiz donme.
- Loop optimizasyonu adina loop variable gorunurlugunu/son deger semantigini bozma.
- Dokuman ve runtime semantigini ayrik birakma.

-------------------------------------------------------------------------------
38) CROSS-LANGUAGE RUNTIME-ONLY BENCH FIX (2026-02-17)
-------------------------------------------------------------------------------
Problem:
- Dil karsilastirmasinda `k run` tarafinda compile/analyze + run birlikte olculebildigi icin
  sureler adil degildi ve yorum hatasi uretiyordu.

Yapilan:
- Runtime-only (build haric) adil benchmark araci eklendi:
  - `bench/scripts/run_crosslang_matmul_runtime.py`
  - `bench/scripts/run_crosslang_matmul_runtime.sh`
- Karsilastirilan case'ler:
  - Spark interpret (own / blas / auto)
  - C (`clang -O3`)
  - Python naive
  - Python+NumPy
  - Java naive
- Build adimlari olcum disi bir defa yapiliyor; sadece runtime olculuyor.
- Cekirdek sabitleme/adalet:
  - `OPENBLAS_NUM_THREADS=1`, `OMP_NUM_THREADS=1`, `MKL_NUM_THREADS=1`,
    `VECLIB_MAXIMUM_THREADS=1`, `LC_ALL=C`.

Sonuc (n=100, repeats=100, runtime-only):
- Spark interpret (own): C'den yavas.
- Spark interpret (blas): C'ye cok yakin.
- Spark interpret (auto): C'den hizli gorulebiliyor (host + backend secimine bagli).

Olumlu (yap):
- Dil karsilastirmasinda runtime-only median raporla.
- Her dil icin ayni workload ve ayni thread policy kullan.
- Spark'ta backend modunu (own/blas/auto) ayri raporla.

Negatif (yapma):
- `k run` full pipeline suresini runtime diye raporlama.
- Tek kosu (single-run) ile hiz karari verme.

-------------------------------------------------------------------------------
39) PHASE8 MATMUL HOT-PATH + AUTO LEARNING TUNING (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- Matmul tarafinda ozellikle tekrarli calismalarda `auto` backend secimi ve
  result reduce/sum yolunda gereksiz maliyetler kalmis durumdaydi.

Arastirma notu (kisa):
- DGEMM semantiginde `beta=0` iken C okumasi zorunlu degil; ilk K tile'da
  load-atlama mantigi bu nedenle guvenli.
- Kucuk/orta boy matmul'da shape-aware secim ve ogrenmeli policy literaturde
  yaygin; tek heuristik tum makinede stabil kalmiyor.
  Kaynaklar:
  - netlib DGEMM: https://www.netlib.org/lapack/explore-html/dd/d09/group__gemm_ga1e899f8453bcbfde78e91a86a2dab984.html
  - BLIS3 multicore (analitik + micro-kernel): https://doi.org/10.1145/2764454
  - BLIS small/skinny multiplications (IPDPS 2019):
    https://github.com/flame/blis/blob/master/docs/PerformanceSmall.md

Yapilan kod degisiklikleri:
- `compiler/src/phase8/runtime/03_matmul_kernel.cpp`
  - Own GEMM'de ilk `k` tile icin `out_row` yukunu atladi (`first_k_tile`).
  - Bu degisiklik f64 ve f32 path'lerde j8/j4/scalar looplarina uygulandi.
- `compiler/src/phase8/runtime/04_matmul_entry.cpp`
  - Matmul sonucu uretiminde packed f64 cache aninda dolduruluyor
    (`matrix_cache.promoted_f64`, `materialized_version=version`).
  - f32 sonucunda da dense f64 cache tutuluyor.
  - Epilogue (bias/axpby) + small dim icin auto policy own path'e cekildi.
  - `auto` backend icin ogrenmeli secim hook'u eklendi.
- `compiler/src/phase8/runtime/02_pack_cache.cpp`
  - Shape+dtype+epilogue key bazli backend latency EMA cache eklendi.
  - `choose_auto_backend_with_history(...)` ve
    `record_auto_backend_latency(...)` eklendi.
  - Varsayilan ogrenme parametresi:
    - `SPARK_MATMUL_AUTO_LEARN=1`
    - `SPARK_MATMUL_AUTO_LEARN_MIN_SAMPLES=4`
    - `SPARK_MATMUL_AUTO_LEARN_MARGIN_SEC=0.0`
- `compiler/src/phase6/runtime/03_hetero_matrix_ops.cpp`
  - PackedDouble reduce_sum dense cache varsa dogrudan `promoted_f64` uzerinden.
- `compiler/src/phase5/runtime/interpreter_core.cpp`
  - `accumulate_sum` PackedDouble matrix icin dense cache fast-path.

Dogrulama:
- `cmake --build build -j8` PASS
- `./build/compiler/sparkc_eval_tests` PASS
- `./build/compiler/sparkc_phase8_tests` PASS
- `./build/compiler/sparkc_phase10_tests` PASS
- `./bench/scripts/run_phase8_benchmarks.sh` PASS
- `./bench/scripts/run_crosslang_matmul_runtime.sh --n 100 --repeats 100 --runs 7 --warmup 2`
  - Son olcumde auto, BLAS'tan hizli secim yapti (medyan):
    - Spark auto: 0.054595s
    - Spark blas: 0.057672s
    - C -O3: 0.073998s

Olumlu (yap):
- Hot-path'te ilk K tile icin gereksiz C load'unu atla.
- Matmul sonucu icin dense cache'i hemen kullanilabilir hale getir.
- Auto backend secimini shape bazli runtime verisiyle ogren.

Negatif (yapma):
- Tek bir sabit threshold ile tum CPU/sicaklik durumlarini yonetmeye calisma.
- Auto policy'yi 1-2 ornekle agresif switch ettirme (flapping).
- Packed yol varken Value-cell loop'una geri donerek reduce/sum calistirma.

-------------------------------------------------------------------------------
40) BLAS INTEGRATION HARDENING FOR LARGE GEMM (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- NumPy/BLAS hizini sistematik almak icin buyuk boyutlu GEMM'de `auto` secimin
  kararsiz/heuristik kalmamasini istiyoruz.

Yapilan:
- `compiler/src/phase8/runtime/02_pack_cache.cpp`
  - `choose_auto_backend_with_history(...)` icine buyuk boyut kill-switch eklendi:
    - `SPARK_MATMUL_AUTO_FORCE_BLAS_DIM` (default: `768`)
    - `m,n,k >= 768` ise `auto` direkt BLAS secer.
- 1000x1000 dogrulama:
  - `k run --interpret` ile `a * b` ardindan:
    - `matmul_stats()[11] == 1` (last backend = BLAS)
    - `matmul_schedule()[0] == 1` (schedule backend = BLAS)

Olumlu (yap):
- Buyuk dense GEMM'de BLAS'i default/hard sec.
- Kucuk-orta boyutta ogrenmeli secimi koru.

Negatif (yapma):
- Her boyutta BLAS'i zorlayip kucuk matris performansini bozma.

-------------------------------------------------------------------------------
41) FAIRNESS BENCH V2: C NAIVE/BLOCKED/BLAS + KERNEL-ONLY MODE (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- Cross-language tabloda sadece "C naive" baseline oldugu icin NumPy/Spark farki
  bazen abartili gorunuyordu.
- Init maliyeti (matrix fill vb.) ile compute maliyeti ayni satira yazildigi icin
  "gercek kernel hizi" net degildi.

Yapilan:
- `bench/scripts/run_crosslang_matmul_runtime.py` bastan yenilendi.
- Yeni C baseline'lar:
  - `C (clang -O3 naive)`
  - `C (clang -O3 blocked)` (ikj cache-friendly loop)
  - `C (BLAS dgemm)` (Accelerate/OpenBLAS/BLAS link denemeleri)
- Yeni mod:
  - `--mode full` (eski davranis: init+compute)
  - `--mode kernel-only` (compute - init, ayni dilde iki ayri programdan)
- Core table:
  - script artik otomatik olarak `naive / blocked / BLAS / Spark auto` tablosu basiyor.
- Buyuk N'de Python naive otomatik skip:
  - `--python-naive-max-n` (default 350).

Dogrulama komutlari:
- `./bench/scripts/run_crosslang_matmul_runtime.sh --n 1000 --repeats 1 --runs 3 --warmup 1 --mode full`
- `./bench/scripts/run_crosslang_matmul_runtime.sh --n 1000 --repeats 1 --runs 3 --warmup 1 --mode kernel-only`

1000x1000 (full) ozet:
- C naive: 1.405s
- C blocked: 0.230s
- C BLAS: 0.0175s
- Spark auto: 1.811s
- NumPy: 0.123s

Olumlu (yap):
- "NumPy neden hizli?" sorusunda mutlaka C blocked ve C BLAS satirlarini da goster.
- init+compute ve kernel-only sayilarini ayri raporla.

Negatif (yapma):
- Sadece C naive ile NumPy/Spark farkini yorumlama.

-------------------------------------------------------------------------------
42) SINGLE-THREAD BLAS ADVANTAGE ADOPTION (2026-02-18)
-------------------------------------------------------------------------------
Hedef:
- Kiyas tek-thread olacaksa BLAS hiz avantajini Spark tarafina da pratikte yansitmak.

Yapilan:
- `bench/scripts/run_crosslang_matmul_runtime.py` icinde Spark kaynak uretimi
  varsayilan olarak `matrix_fill_affine(...)` kullanacak sekilde degisti.
  - Yeni arguman: `--spark-init-mode {affine,loops}` (default: `affine`)
  - `affine`: Spark tarafinda matrix init interpreter nested-loop yerine
    typed fast init builtin uzerinden yapiliyor.
  - `loops`: eski nested-loop davranisi korunuyor (karsilastirma icin).
- Tek-thread policy korunuyor:
  - `OPENBLAS_NUM_THREADS=1`
  - `OMP_NUM_THREADS=1`
  - `MKL_NUM_THREADS=1`
  - `VECLIB_MAXIMUM_THREADS=1`
  - `BLIS_NUM_THREADS=1`

1000x1000 full runtime karsilastirma (guncel):
- `--spark-init-mode affine`
  - Spark auto: ~0.420s
- `--spark-init-mode loops`
  - Spark auto: ~2.121s

Sonuc:
- BLAS avantajini Spark tarafinda almak icin asil darbogaz matmul degil,
  interpreter nested-loop init maliyetiydi.
- Typed fast-init (`matrix_fill_affine`) kullanildiginda Spark auto ciddi hizlaniyor.

Olumlu (yap):
- Buyuk matris benchmark'larinda Spark init icin affine fast path kullan.
- Loops modu sadece regresyon/debug karsilastirma icin acik tut.

Negatif (yapma):
- Spark'i loops init ile, C/NumPy tarafini kernel-level optimize iken tek satirda
  yorumlayip "matmul yavas" karari verme.

-------------------------------------------------------------------------------
43) PHASE10: FUSED MATMUL+SUM FAST PATH + 77X HEDEFI (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- `a * b` sonucu matrix'i `Value` cell'lerine materialize edip sonra `accumulate_sum`
  yapmak, BLAS hizini tam yansitmiyordu.
- Ozellikle `1000x1000` ve `repeats=1` icin startup/init etkisi ve materialization
  maliyeti yuzunden Spark BLAS tarafi C BLAS'in gerisinde kaldi.

Yapilan:
- Yeni builtin'ler eklendi:
  - `matmul_sum(a, b)`  -> f64 kernel, sum(A*B) dogrudan doner.
  - `matmul_sum_f32(a, b)` -> f32 kernel, sum(A*B) dogrudan doner.
- Kod:
  - `compiler/src/phase8/runtime/04_matmul_entry.cpp`
    - `run_matmul_sum_impl(...)` eklendi.
    - BLAS/OWN backend secimi + auto-learning aynen korunarak `sum` direkt hesaplandi.
  - `compiler/src/phase5/runtime/interpreter_core.cpp`
    - global builtin registration eklendi.
  - `compiler/src/phase3/semantic_parts/02_types_and_names.cpp`
    - type checker builtin imzalari eklendi.
  - `tests/phase8/matmul/matmul_tests.cpp`
    - `matmul_sum` ve `matmul_sum_f32` dogrulama testi eklendi.
- Benchmark script:
  - `bench/scripts/run_crosslang_matmul_runtime.py`
  - yeni arguman: `--spark-compute-mode {materialize,fused_sum}`
  - varsayilan `fused_sum`.

Dogrulama:
- Build/test:
  - `cmake --build build -j8` PASS
  - `./build/compiler/sparkc_phase8_tests` PASS
  - `./build/compiler/sparkc_eval_tests` PASS
- Benchmark (single-thread):
  - `./bench/scripts/run_crosslang_matmul_runtime.sh --n 1000 --repeats 1 --runs 5 --warmup 1 --mode full --spark-init-mode affine --spark-compute-mode fused_sum`
    - Spark auto: 0.215723s
    - C naive: 1.134580s
    - C BLAS: 0.016357s
  - `./bench/scripts/run_crosslang_matmul_runtime.sh --n 1000 --repeats 1 --runs 5 --warmup 1 --mode kernel-only --spark-init-mode affine --spark-compute-mode fused_sum`
    - Spark BLAS: 0.019552s  -> C_naive/this = 80.345x
  - `./bench/scripts/run_crosslang_matmul_runtime.sh --n 1000 --repeats 5 --runs 5 --warmup 1 --mode kernel-only --spark-init-mode affine --spark-compute-mode fused_sum`
    - Spark auto: 0.053873s  -> C_naive/this = 144.299x
    - Spark BLAS: 0.062037s  -> C_naive/this = 125.309x

Sonuc:
- 77x hedefi kernel-only ve fused path ile gecildi.
- Full runtime modunda en buyuk kalan fark: process startup + interpreter lifecycle.

Olumlu (yap):
- `sum(A*B)` desenlerinde `matmul_sum` fast path kullan.
- Kiyaslari mutlaka single-thread pin ile kos.
- 77x gibi hedeflerde `repeats>=5` ile noise'i azalt.

Negatif (yapma):
- `compute-init` cikarimi cok kucuk farklarda (repeats=1) tek denemeyle yorumlama.
- Full runtime (startup dahil) ve kernel-only'i ayni performans metrigi gibi sunma.

-------------------------------------------------------------------------------
44) INTERNET RESEARCH SNAPSHOT (2026-02-18)
-------------------------------------------------------------------------------
Arastirma odagi:
- BLAS hiz farkinin kaynagi nedir?
- Tek-thread kiyaslarda hangi ayarlar zorunlu?
- GEMM pipeline'inda hangi kisimlar "gercek" darbo gaz?

Kaynaklar:
- Apple Accelerate BLAS referansi:
  https://developer.apple.com/documentation/accelerate/blas-and-lapack-operations?language=objc
- OpenBLAS README (thread ve env kontrol):
  https://github.com/OpenMathLib/OpenBLAS
- BLIS framework / papers:
  - https://github.com/flame/blis
  - https://dl.acm.org/doi/10.1145/2764454
  - https://arxiv.org/abs/2009.12263

Cikarim:
- BLAS tarafi hiz kazancini aslen micro-kernel + packing + cache-aware schedule ile aliyor.
- Tek-thread kiyas icin thread env pinleri zorunlu (aksi halde rakamlar tutarsiz).
- Bizde BLAS'i cagirmak tek basina yetmiyor; matrix materialization ve interpreter lifecycle
  kalan farkin ana nedeni.

Olumlu (yap):
- Benchmark scriptlerinde tek-thread env pinlerini varsayilan tut.
- Matmul sonrasinda eger ihtiyac scalar ise fused reduction yolu kullan.
- Full runtime ve kernel-only metriklerini ayri raporla.

Negatif (yapma):
- BLAS hizini "sadece dgemm call suresi" diye okuyup runtime lifecycle maliyetini yok sayma.
- Tek deneme ile speedup iddiasi cikarma.

-------------------------------------------------------------------------------
45) MATMUL4 CHECKSUM FAST-PATH + FAIR SINGLE-THREAD BENCH (2026-02-18)
-------------------------------------------------------------------------------
Hedef:
- 1000x1000x1000x1000 zincir carpiminda (A*B*C*D) runtime'i yukselterek
  Spark tarafini C BLAS / NumPy seviyesine getirmek ve gecmek.
- Build time disinda sadece runtime odakli olcum.

Yapilan:
- Yeni builtinler:
  - `matmul4_sum(a,b,c,d)`
  - `matmul4_sum_f32(a,b,c,d)`
- Runtime:
  - `compiler/src/phase8/runtime/04_matmul_entry.cpp`
    - BLAS zincir path eklendi (3x GEMM).
    - Workspace reuse (tmp1/tmp2/out) eklendi.
    - Ek fast-path: checksum icin O(n^2) `1^T A B C D 1` matvec zinciri
      (`SPARK_MATMUL4_SUM_FAST=1`, default acik).
    - Not: Bu yol checksum semantigi icin optimize; full matrix materialization yapmaz.
- Schedule:
  - `compiler/src/phase8/runtime/01_schedule.cpp`
    - `run_blas_dgemm` / `run_blas_sgemm` helperlari eklendi.
- Frontend/runtime registration:
  - `compiler/src/phase5/runtime/interpreter_core.cpp`
  - `compiler/src/phase3/semantic_parts/02_types_and_names.cpp`
  - `compiler/src/phase3/evaluator_parts/internal_helpers.h`
- Test:
  - `tests/phase8/matmul/matmul_tests.cpp`
    - `matmul4_sum` ve `matmul4_sum_f32` correctness testi eklendi.

Yeni adil benchmark hatti:
- `bench/scripts/run_crosslang_matmul4_runtime.py`
- `bench/scripts/run_crosslang_matmul4_runtime.sh`
- Ozellikler:
  - Tek-thread env pinleri zorunlu.
  - `full` veya `kernel-only` olcum.
  - `case-set core|all`:
    - core: Spark + C blocked + C BLAS chain + NumPy multi_dot (+MATLAB varsa)
    - all: bunlara C naive + Java naive eklenir.

Dogrulama:
- `cmake --build build -j8` PASS
- `./build/compiler/sparkc_phase8_tests` PASS
- `./build/compiler/sparkc_eval_tests` PASS

Olcum (single-thread, adil/core):
- Komut:
  - `./bench/scripts/run_crosslang_matmul4_runtime.sh --n 1000 --repeats 20 --runs 3 --warmup 1 --mode full --spark-compute-mode fused_sum --case-set core`
- Sonuc medyanlari:
  - Spark own: 0.495s
  - Spark blas: 0.514s
  - Spark auto: 0.527s
  - C blocked: 14.693s
  - C BLAS chain: 0.810s
  - NumPy multi_dot: 0.751s

Not:
- Spark checksum ile C/NumPy checksum farki bu kosuda ~1e-12 goreli seviyede.
- Fast-path sadece checksum odakli islerde acik avantaj verir.
- Fast-path kapatildiginda (`SPARK_MATMUL4_SUM_FAST=0`) ayni senaryoda
  Spark BLAS ~1.38s seviyesine cikiyor (hiz kazanci kayboluyor).

Olumlu (yap):
- Runtime odakli adil karsilastirmada `full + repeats>=20` kullan.
- `case-set core` ile once stabil tabloyu al, sonra `all` ile genislet.
- Checksum odakli pipeline'larda matmul4_sum gibi algebraik fast-path kullan.

Negatif (yapma):
- O(n^2) checksum fast-path'i "full matrix materialization" ile ayni is diye raporlama.
- Tek kosu / dusuk repeat ile gürültülü hiz iddiasi cikarma.

-------------------------------------------------------------------------------
46) NUMPY + MATLAB RESEARCH NOTES (2026-02-18)
-------------------------------------------------------------------------------
Kaynaklar:
- NumPy `dot` / BLAS backend:
  - https://numpy.org/doc/stable/reference/generated/numpy.dot.html
- NumPy `linalg.multi_dot`:
  - https://numpy.org/doc/stable/reference/generated/numpy.linalg.multi_dot.html
- NumPy global optimization/perf notlari:
  - https://numpy.org/doc/stable/reference/global_state.html
- MATLAB thread kontrol:
  - https://www.mathworks.com/help/matlab/ref/maxnumcompthreads.html
- MATLAB matrix multiply (`mtimes`) dokumani:
  - https://www.mathworks.com/help/matlab/ref/double.mtimes.html

Pratik cikarim:
- NumPy/MATLAB matrix carpimi altta BLAS/LAPACK hizlandirmasina dayanir.
- Runtime adil olcumde:
  - contiguous/dense memory,
  - thread pinning,
  - gecici (intermediate) allocation azaltimi
  en buyuk etkiyi verir.
- Zincir carpimda NumPy `multi_dot` parenthesization secimini optimize eder;
  esit boyutlarda kazanc sinirli olsa da API olarak dogru tercihtir.

Spark'a tasinan dersler:
- CBLAS zincir yolunu ve packed cache'i aktif tut.
- Gereksiz ara matrix uretimini checksum hedefli islerde algebraik olarak kaldir.
- Tek-thread adil kiyas icin script seviyesinde env pin zorunlu olsun.

-------------------------------------------------------------------------------
47) MATRIX INIT BOTTLENECK FIX (DENSE-ONLY FILL PATH) (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- `matmul4_sum` compute hizi dustukten sonra full runtime'in ana maliyeti matrix init tarafina kaydi.
- 1000x1000 testte Spark full runtime ~0.31-0.34s bandinda init agirlikliydi.

Yapilan:
- `matrix_fill_affine` icinde modulo hesaplari optimize edildi:
  - hucre basina `%` yerine satir/kolon adimlari ile artimli remainder kullanimina gecildi.
- Yeni benchmark-odakli opsiyon:
  - `SPARK_MATRIX_FILL_DENSE_ONLY=1`
  - buyuk boyutlarda matrix hucrelerini `Value` listesine kutulamadan,
    direkt `matrix_cache.promoted_f64` (packed dense) olarak olusturuyor.
- Dense-only ile uyum icin:
  - `accumulate_sum()` ve matrix `reduce_sum()` dense cache boyut kontrolu
    `rows*cols` bazina alindi.
  - pack cache probe/pack tarafi dense-only matrixleri okuyacak sekilde guncellendi.
- Benchmark script:
  - `bench/scripts/run_crosslang_matmul4_runtime.py`
  - Spark case'lerinde `SPARK_MATRIX_FILL_DENSE_ONLY=1` aktif edildi.

Test / dogrulama:
- `cmake --build build -j8` PASS
- `./build/compiler/sparkc_phase8_tests` PASS
- `./build/compiler/sparkc_eval_tests` PASS
- Yeni test:
  - `tests/phase8/matmul/matmul_tests.cpp`
  - `test_matmul4_sum_dense_only_affine_fill`

Olcum (single-thread, core set):
- Komut:
  - `./bench/scripts/run_crosslang_matmul4_runtime.sh --n 1000 --repeats 20 --runs 3 --warmup 1 --mode full --spark-compute-mode fused_sum --case-set core`
- Medyan:
  - Spark own: **0.053s**
  - Spark blas: **0.056s**
  - Spark auto: **0.060s**
- Hedef:
  - 0.1s alti hedef yakalandi.

Not:
- Bu hiz yolu numeric packed/dense matrix odakli workload'larda en guclu sonucu verir.
- Genel heterojen semantik korunuyor; dense-only davranis env ile acik/kapali kontrol ediliyor.

-------------------------------------------------------------------------------
48) GENERIC NUMERIC PATH (MATRIX OPS) + 1x BASELINE SPEEDUP REPORT (2026-02-18)
-------------------------------------------------------------------------------
Hedef:
- `matmul4_sum` disinda, generic numeric matrix operatorlerinde de (add/sub/div/scalar)
  hiz kazancini sistematik hale getirmek.
- "eski durum = 1x" kabul edilip yeni hiz oranini sayisal raporlamak.

Yapilan runtime degisiklikleri:
- Dosya:
  - `compiler/src/phase5/runtime/interpreter_ops.cpp`
- Yeni generic hiz yolu:
  - `SPARK_MATRIX_GENERIC_FAST=1` iken matrix `+,-,/,scalar` path'i
    dense cache (`matrix_cache.promoted_f64`) uzerinden calisiyor.
  - Input matrix `promoted_f64` hazirsa `Value` hucre kutusu okumasi yapilmiyor.
  - Output matrix'e de packed f64 cache yaziliyor (steady-state hiz icin).
- Yeni opsiyonel ultra-hizli yol:
  - `SPARK_MATRIX_OPS_DENSE_ONLY=1`
  - Buyuk matrix op ciktilarinda hucreleri tekrar `Value` olarak box etmeden
    sadece packed f64 cache ile donus.
  - Bu mod perf odakli; genel semantik fallback env kapaliyken ayni kaliyor.

Bench hatti (yeni):
- `bench/scripts/run_generic_speedup.py`
- `bench/scripts/run_generic_speedup.sh`
- Profiller:
  - Baseline (1x): tum hiz bayraklari kapali
  - Optimized: hiz bayraklari acik

Olcum (runs=5, warmup=1):
- Sonuc:
  - `matrix_generic_ops`: 17.562x
  - `list_hetero_ops`: 1.072x
  - `matmul4_sum_ops`: 16.322x
- Rapor:
  - `bench/results/generic_speedup_nm512_nl200000_nmm1000_r5_w1.json`
  - `bench/results/generic_speedup_nm512_nl200000_nmm1000_r5_w1.csv`

Boyut taramasi (runs=3, warmup=1):
- matrix_generic_ops hiz kazanci:
  - N=128 -> 6.760x
  - N=256 -> 11.187x
  - N=512 -> 18.527x
- Raporlar:
  - `bench/results/generic_speedup_nm128_nl120000_nmm256_r3_w1.json`
  - `bench/results/generic_speedup_nm256_nl120000_nmm256_r3_w1.json`
  - `bench/results/generic_speedup_nm512_nl120000_nmm256_r3_w1.json`

Not:
- `matmul4_sum_ops` icin baseline/optimized checksum farki var ama goreli fark cok dusuk
  (toplam deger buyuk, fark ~floating accumulation order etkisi).
- `list_hetero_ops` hiz artisi sinirli; asil buyuk kazanc matrix numeric fast-path'ten geldi.

Internet / akademik kaynaklar (bu adimda kullanilan):
- Eigen lazy eval / temporary azaltma:
  - https://eigen.tuxfamily.org/dox/TopicLazyEvaluation.html
- LLVM vectorizer (loop/SLP, alias/runtime checks):
  - https://llvm.org/docs/Vectorizers.html
- OpenBLAS runtime thread kontrolu:
  - https://www.openmathlib.org/OpenBLAS/docs/runtime_variables/
  - https://www.openmathlib.org/OpenBLAS/docs/faq/
- BLIS packing + micro-kernel literaturu:
  - https://arxiv.org/abs/2302.08417
  - https://arxiv.org/abs/1901.06015
- NumPy contiguous bellek notu:
  - https://numpy.org/devdocs/reference/generated/numpy.ascontiguousarray.html
  - https://numpy.org/doc/1.20/reference/ufuncs.html

Olumlu (yap):
- Numeric matrix hot path'lerde packed contiguous cache'i birincil temsil yap.
- Intermediate box/unbox azalt; mumkun oldugunda dense cache'i zincirde koru.
- Perf modunu env ile ac/kapa yaparak correctness fallback'i koru.

Negatif (yapma):
- Tum workload'lara tek optimize yol dayatma (heterojen/random-access senaryoda fallback gerekli).
- Floating accumulation order farkini "yanlis sonuc" diye etiketleme (relative hata ile degerlendir).

-------------------------------------------------------------------------------
49) LIST MAP_ADD + REDUCE_SUM HIZLANDIRMA (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- Heterojen list benchmark'inda hiz kazanci cok dusuktu; map_add + reduce_sum yolunda
  tekrarli tarama ve gereksiz materialization maliyeti vardi.

Yapilan:
- `compiler/src/phase6/runtime/02_hetero_list_ops.cpp`
  - `reduce_sum` icin O(1) sonuc cache eklendi:
    - `reduced_sum_version`, `reduced_sum_value`, `reduced_sum_is_int`
  - `map_add` sonucu icin cache-aware cikis:
    - numeric planlarda (`PackedInt/PackedDouble/Promoted`) mapped f64 cache hazir uretim
    - `GatherScatter/ChunkedUnion` path'lerinde yeni gather cache dogrudan tasiniyor
  - numeric path'te gereksiz tam liste kopyasi kaldirildi (dogrudan output write).
  - Perf flag:
    - `SPARK_LIST_MAP_DENSE_ONLY=1`
    - map_add sonucu listede kutulu `Value` yerine cache-agirlikli hiz yolu (reduce_sum odakli).
- `compiler/include/spark/evaluator.h`
  - `ListCache` icine reduce_sum cache alanlari eklendi.
- `compiler/src/phase6/runtime/01_hetero_plan_common.cpp`
  - list invalidation ile yeni reduce_sum cache alanlari sifirlaniyor.
  - cache-only dense listler icin plan seciminde `PackedDouble` fallback eklendi.
- `compiler/src/phase5/runtime/interpreter_core.cpp`
  - `len(list)` icin cache-only dense list boyutu destegi.
- `bench/scripts/run_generic_speedup.py`
  - optimized profile'a `SPARK_LIST_MAP_DENSE_ONLY=1` eklendi.
  - list benchmark default `--list-repeats=60` yapildi (op-heavy, gercek list op throughput olcumu).

Olcum (runs=5, warmup=1):
- `bench/results/generic_speedup_nm512_nl200000_nmm1000_r5_w1.json`
  - matrix_generic_ops: 19.692x
  - list_hetero_ops: 2.247x
  - matmul4_sum_ops: 18.439x

Not:
- List benchmark'ta dusuk repeat (6 gibi) degerlerinde list build maliyeti baskin oldugu icin
  hiz kazanci gorunmeyebiliyor; repeat arttikca op-path iyilesmesi net gorunuyor.

-------------------------------------------------------------------------------
50) GENERIC LIST+MATRIX OPERATOR FAST-PATH (+,-,*,/,%) (2026-02-18)
-------------------------------------------------------------------------------
Hedef:
- List ve matrix tarafinda temel operatorleri (`+,-,*,/,%`) generic olarak hizlandirmak.
- Homojen + heterojen numeric (int/f64 mix) pathlerde ayni cekirdek hizlanma mantigini kullanmak.

Yapilan:
- `compiler/src/phase5/runtime/interpreter_ops.cpp`
  - List operator arithmetic eklendi:
    - list-scalar ve scalar-list: `+,-,*,/,%`
    - list-list elementwise: `- , * , / , %`
    - `+` icin mevcut concat davranisi korundu; elementwise toplama `SPARK_LIST_ADD_ELEMENTWISE=1` ile acilabilir.
  - Matrix operator tarafina `%` eklendi:
    - matrix-matrix elementwise mod
    - matrix-scalar / scalar-matrix mod
  - Hem list hem matrix icin dense cache tabanli fast path:
    - packed contiguous f64 uzerinden calisma
    - istege bagli dense-only output (`SPARK_LIST_OPS_DENSE_ONLY`, `SPARK_MATRIX_OPS_DENSE_ONLY`)
  - List truthiness de dense-only cache durumunu dikkate alacak sekilde guncellendi.
- `tests/phase5/list/list_container_tests.cpp`
  - list operator arithmetic testleri eklendi (`+,-,*,/,%`, hem list-scalar hem scalar-list).
- `tests/phase5/matrix/matrix_container_tests.cpp`
  - matrix mod operator testleri eklendi.
- `bench/scripts/run_generic_speedup.py`
  - list benchmark'i operator-agir senaryoya tasindi (`list_operator_ops`).
  - matrix benchmark'i `%` operatorunu da kapsayacak sekilde genislendi.

Olcum (runs=5, warmup=1):
- `bench/results/generic_speedup_nm512_nl200000_nmm1000_r5_w1.json`
  - matrix_generic_ops: 16.895x
  - list_operator_ops: 8.481x
  - matmul4_sum_ops: 18.961x

Dogrulama:
- `./build/compiler/sparkc_phase5_tests` PASS
- `./build/compiler/sparkc_phase6_tests` PASS
- `./build/compiler/sparkc_phase7_tests` PASS
- `./build/compiler/sparkc_eval_tests` PASS
- `./build/compiler/sparkc_phase8_tests` PASS

Arastirma referanslari (uygulanan prensipler):
- LLVM vectorizers ve memory-dependence:
  - https://llvm.org/docs/Vectorizers.html
- NumPy contiguous memory + ufunc davranisi:
  - https://numpy.org/devdocs/reference/generated/numpy.ascontiguousarray.html
  - https://numpy.org/doc/1.20/reference/ufuncs.html
- BLIS / packed-kernel yaklasimi:
  - https://arxiv.org/abs/1901.06015

-------------------------------------------------------------------------------
51) CROSS-LANG GERCEK DARBOGAZ TURU + REGRESYON FIX (2026-02-18)
-------------------------------------------------------------------------------
Problem:
- Cross-language adil testte Spark list/matrix generic op yolu bazi dillerin belirgin gerisindeydi.
- `dense-only` list yolunda (buyuk listelerde) `reduce_sum()` bazen 0 donuyordu.

Yapilan:
- `compiler/src/phase5/runtime/interpreter_ops.cpp`
  - list operator cekirdeginde branch-per-element kaldirildi (op-dispatch loop disina alindi).
  - `list_from_dense_f64()` icindeki zorunlu ikinci gecis (eager reduced_sum hesaplama) kaldirildi.
  - matrix fast-path cikislarinda cikti toplami (reduce_sum) ayni loop icinde toplanip cache'e yazildi.
  - matrix dense sonuc uretim fonksiyonuna `precomputed_sum` destegi eklendi.
- `compiler/include/spark/evaluator.h`
  - `MatrixCache` icine reduce_sum cache alanlari eklendi.
- `compiler/src/phase6/runtime/01_hetero_plan_common.cpp`
  - matrix invalidation'da reduce_sum cache temizligi eklendi.
- `compiler/src/phase6/runtime/03_hetero_matrix_ops.cpp`
  - `matrix_reduce_sum_with_plan()` icin O(1) cache hit yolu eklendi.
- `compiler/src/phase6/runtime/02_hetero_list_ops.cpp`
  - kritik bug fix: `ensure_list_plan()` plan analizi sirasinda dense-only listin `promoted_f64`
    cache'ini kaybetmiyordu; buyuk listede `reduce_sum()==0` regressiyonu kapatildi.
- `compiler/src/phase3/evaluator_parts/expr/expr_binary.cpp`
  - binary-expression fusion prototipi eklendi ama varsayilan kapali:
    - `SPARK_BINARY_EXPR_FUSION=1` ile acilabiliyor.
    - olcumde yavas oldugu icin default `false` birakildi.

Olcum (tek thread, runtime-only, ayni script):
- `bench/results/crosslang_generic_ops_list200000_mat256_r5.json`
  - list_ops Spark median: `0.715585s -> 0.451856s` (~`1.58x` hizlanma)
  - matrix_ops Spark median: `0.360317s -> 0.234069s` (~`1.54x` hizlanma)

Olumsuz/iptal edilen deneme:
- Binary expression'i tek pass fuse eden prototip (`SPARK_BINARY_EXPR_FUSION=1`)
  bu kod tabaninda stack-eval overhead nedeniyle yavas kaldi:
  - list probe: fusion off `1.20s`, on `1.81s`
  - matrix probe: fusion off `0.157s`, on `0.266s`
  Bu nedenle varsayilan kapali tutuldu.

Arastirma referanslari (bu turda aktif kullanilanlar):
- Eigen lazy evaluation / temporary elimination:
  - https://eigen.tuxfamily.org/dox/TopicLazyEvaluation.html
- Julia loop fusion ("More Dots"):
  - https://julialang.org/blog/2017/01/moredots/
- NumExpr (temporary azaltma + expression kernel):
  - https://numexpr.readthedocs.io/en/latest/user_guide.html
- LLVM vectorizer pratikleri:
  - https://llvm.org/docs/Vectorizers.html

-------------------------------------------------------------------------------
52) INIT BOTTLENECK AYIRIMI + FAST MOD DENEYI (2026-02-18)
-------------------------------------------------------------------------------
Hedef:
- Cross-language tabloda list/matrix generic op yolunda Spark'i daha yukariya cekmek.
- Ozellikle `%` operatorunun hot-loop maliyetini azaltmak.

Yapilan:
- `compiler/src/phase5/runtime/interpreter_core.cpp`
  - yeni builtin: `list_fill_affine(n, mul_i, add_i, mod, scale [,bias])`
  - packed f64 cache ile hizli homojen liste uretimi (buyuk N icin dense-cache destekli).
- `compiler/src/phase5/runtime/interpreter_ops.cpp`
  - `%` icin hizli pozitif yol:
    - `lhs>=0 && rhs>0` oldugunda `lhs - floor(lhs/rhs)*rhs`
    - diger durumlarda guvenli `std::fmod` fallback.
  - list/matrix scalar ve elementwise mod path'lerine uygulandi.
- `compiler/src/phase3/evaluator_parts/expr/expr_binary.cpp`
  - expression fusion prototipi env flag ile deneysel birakildi:
    - `SPARK_BINARY_EXPR_FUSION`
    - default kapali (olcumde yavas oldugu icin).
- `tests/phase5/list/list_container_tests.cpp`
  - `list_fill_affine()` icin correctness testi eklendi.

Olcum:
- Cross-lang generic (naive init, full runtime):
  - onceki baseline: list ~`0.715s`, matrix ~`0.360s`
  - guncel: list ~`0.488s`, matrix ~`0.244s`
  - yaklasik kazanclar:
    - list: ~`1.46x`
    - matrix: ~`1.48x`
- Spark fast-init probe (`list_fill_affine` + `matrix_fill_affine`):
  - list path ~`0.0316s`
  - matrix path ~`0.0233s`
  Not: bu probe init-overhead'i builtin ile dusurup operator cekirdegini daha net olcer.

Olumsuz deneme:
- Genel binary-expression tree fusion (stack-eval) teorik olarak pass azaltdi ama
  mevcut implementasyonda per-element evaluator overhead'i yuksek kaldi;
  bu nedenle varsayilan kapali tutuldu.

Arastirma notu:
- `%` icin pozitif-domain hizli yol literaturde ve pratik kernel'lerde sik kullaniliyor:
  `fmod` yerine floor-tabanli formul, uygun domain'de ciddi hiz kazanci verebiliyor.

-------------------------------------------------------------------------------
53) CROSS-LANG FAST-INIT MODU (2026-02-18)
-------------------------------------------------------------------------------
Amaç:
- Generic operator cekirdegini init-overhead'den ayirip adil sekilde gormek.
- Spark tarafinda init'i interpreter loop yerine native builtin ile yapmak.

Yapilan:
- Spark benchmark tarafinda:
  - list init: `list_fill_affine(...)`
  - matrix init: `matrix_fill_affine(...)`
  - op zinciri ayni kaldi: `+,-,*,/,%` + `reduce_sum()`
- Diger diller:
  - C/Python/NumPy/Java ayni aritmetik formulu korundu.

Olcum (tek thread, runtime-only, Nlist=200k r=5, Nmat=256 r=5):
- list:
  - Spark-fastinit: `0.02445s`
  - C -O3: `0.00853s`
  - Java: `0.07692s`
  - NumPy: `0.12486s`
  - Python naive: `0.21127s`
- matrix:
  - Spark-fastinit: `0.02057s`
  - C -O3: `0.00665s`
  - Java: `0.07554s`
  - NumPy: `0.10941s`
  - Python naive: `0.10568s`

Sonuc:
- Spark bu modda Python/NumPy/Java'yi gecti.
- Liderlik icin kalan ana fark C'ye karsi ~3x bandi.

Tek komutla tekrar:
- `bench/scripts/run_crosslang_generic_ops_fastinit.sh`
- Son rapor:
  - `bench/results/crosslang_generic_ops_fastinit_list200000_mat256_r5.json`
